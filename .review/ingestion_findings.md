## Ingestion: 27 findings (10 high, 12 med, 5 low)
- [high] MAP-R4 src/wattwise_core/ingestion/adapters/intervals_icu.py:310 â The Intervals.icu source `sub_type` token is written verbatim into the canonical `sub_sport` field with ZERO normalization (not even lowercased) and no vocab->registry mapping. MAP-R4 requires source vocabulary be translated to canonical terms (unknown -> other/null), and MAP-R2 forbids passing a raw source token into a canonical field. The canonical `Activity.sub_sport` column is a FK into the `sub_sport` registry (activity.py:71-72); an arbitrary source token (e.g. "Gravel", "VirtualRide") is both a source-vocabulary leak into a consumer-readable canonical field and a referential-integrity hazard (un-seeded code). | FIX: Map `sub_type` through a canonical sub_sport-code lookup (like `_sport_code`), returning a known registry code or None for unmapped tokens; never pass the raw source token through. | BYTES: "sub_sport": act.sub_type,
- [high] CONF-R5 src/wattwise_core/ingestion/ingest.py:314 â resolve_field is invoked WITHOUT dispute_tolerance (the param defaults to None), so dedup._is_disputed (dedup.py:91 `if tolerance is None ... return False`) always returns False, and the returned ResolvedField.disputed is then discarded â line 316 reads only `winner.value`. The canonical activity write sets no Coverage at all (and streams/wellness write `coverage={}` at lines 250/285). Therefore coverage `disputed:true` is NEVER set on ANY canonical record, even when â¥2 candidates materially disagree. CONF-R5 mandates the resolver set coverage `disputed:true` iff â¥2 contributing candidates materially disagree beyond tolerance (still selecting the best value, never silently hiding the disagreement). The disagreement signal is silently dropped. | FIX: Pass a configured numeric dispute_tolerance into resolve_field per (entity_type, field) and propagate ResolvedField.disputed into a Coverage(disputed=True) written onto the canonical activity/wellness record so the disagreement surfaces (CONF-R5). | BYTES: winner = resolve_field(contributors)
- [high] CONF-R2 src/wattwise_core/ingestion/ingest.py:288 â _write_wellness_canonical writes a SINGLE candidate's wellness values directly onto the DailyWellness row by last-writer-wins setattr â it never calls resolve_field. The ingest loop (lines 97-99) invokes _write_wellness_canonical once per daily_wellness candidate independently, so two sources for the same (athlete_id, local_date) overwrite each other in arrival order with NO trust_profile/confidence/recency/completeness total order. A lower-trust newer candidate will overwrite a higher-trust value for resting_hr_bpm/hrv_rmssd_ms/etc. This violates the single resolve_field total order (CONF-R2) â daily_wellness fields bypass the conflict resolver entirely (also DEDUP-R4: roll-ups must read RESOLVED canonical, but the canonical row here is never resolved across candidates). | FIX: Resolve daily_wellness fields across all contributing candidates for (athlete_id, local_date) via resolve_field (mirror _resolve_scalars / _write_activity_canonical) before writing the canonical row, instead of last-writer-wins setattr from one candidate. | BYTES:         if payload.get(key) is not None:
            setattr(row, key, payload[key])
- [high] PRV-R2 src/wattwise_core/ingestion/ingest.py:118 â PRV-R2: a candidate's lineage envelope MUST be immutable per ingest; a re-ingest that CHANGES the observation MUST append a new candidate/version entry, preserving the prior one for audit and conflict explanation. Here a changed re-ingest mutates the EXISTING row in place (content_hash, payload, trust_profile, confidence overwritten; fetched_at and ingest_run_id always overwritten), destroying the prior version. The `is_superseded` column the model provides for exactly this (and which the canonical query at line 204 filters on) is never set anywhere in the codebase, so the supersession/versioning mechanism is dead. | FIX: On a content_hash change, insert a NEW SourceCandidate version (with the new ingest_run_id/fetched_at/payload) and mark the prior row is_superseded=True, rather than mutating it. The candidate query at _activity_candidates already excludes is_superseded rows, so versioning would be honored downstream once supersession is actually written. | BYTES:         if existing is not None:
            # Unchanged content -> value-level no-op (UPS-R3): only refresh fetch metadata.
            if existing.content_has
- [high] ING-R6 src/wattwise_core/ingestion/ingest.py:91 â Idempotency/single-count (ING-R6, SYN-R4, DEDUP-R1): on re-ingest of an already-resolved candidate, the existing row's `resolved_activity_id` (returned by _persist_candidate) is IGNORED and unconditionally recomputed via the fuzzy matcher. _resolve_activity_id (line 169-172) passes the stored Activity's fingerprint as a hardcoded `None`, so fingerprint identity NEVER matches a stored activity â re-resolution relies entirely on the fuzzy start/duration/sport matcher. If the resolved canonical start_time (set from cross-candidate resolution at line 188) drifts outside the candidate's Â±2h _IDENTITY_WINDOW, or duration/sport resolves past tolerance, the matcher mints a NEW uuid7 (line 174), producing a duplicate canonical activity for the same real session â violating the single-count invariant on re-sync. | FIX: Short-circuit: if the persisted candidate already carries a resolved_activity_id, reuse it instead of re-running the matcher; and pass the stored activity's own fingerprint (not None) at line 171 so fingerprint identity is honored on re-resolution. This makes re-ingest converge to the same activity_id deterministically. | BYTES:             if cand.gbo_type == GboType.ACTIVITY.value:
                activity_id = await self._resolve_activity_id(athlete, cand)
                row.resolve
- [high] ING-R8 src/wattwise_core/ingestion/ingest.py:74 â The ingest write path (the only canonical writer) never captures the verbatim original file nor creates the typed activity_file reference. IngestService.ingest takes only mapped GboCandidates â no original bytes, no object-store handle. The ActivityFile model (persistence/models/activity.py) is defined but instantiated NOWHERE in src/ (rg for ActivityFile(...) outside the model returns nothing). FIL-R1/ING-R8/ACC-8 require every ingested activity with an original recording file to store it verbatim in object storage AND create an activity_file ref to the resolved activity; this MUST is unsatisfied at the epic's core writer. | FIX: Thread the verbatim upload/download bytes into the ingest path and, after the activity id is resolved (ingest.py:92-95), create an ActivityFile row (object_ref from the object store + format + byte_size + content_hash + source_descriptor_id + fetched_at, dedup-idempotent on (resolved activity, source_descriptor_id, content_hash) per FIL-R5). file_upload.map_upload already has raw_bytes â propagate them rather than discarding after the native-id hash. | BYTES:     async def ingest(
        self,
        athlete_id: str | uuid.UUID,
        source_descriptor_id: str | uuid.UUID,
        candidates: list[GboCandidate],
- [high] ING-UPS-R5 src/wattwise_core/ingestion/ingest.py:287 â daily_wellness canonical write is blind last-write-wins per field across sources: _write_wellness_canonical takes a single candidate and overwrites the row field-by-field with no resolve_field and no trust/fidelity ordering. If two sources contribute the same local_date, whichever candidate is processed last clobbers â a lower-trust value can overwrite a higher-trust existing value. This violates ING-UPS-R4 (conflict-resolved BEFORE the canonical write) and ING-UPS-R5/PRV-R6 (a write MUST NOT overwrite a higher-trust existing value with a lower-trust new value). Activities go through resolve_field; wellness does not. | FIX: Resolve daily_wellness the same way as activities: collect all non-superseded candidates for (athlete_id, local_date), run resolve_field per field over FieldCandidates carrying their trust_tier, and write only the winner â never an unconditional setattr from one candidate. | BYTES:     for key in ("resting_hr_bpm", "hrv_rmssd_ms", "hrv_sdnn_ms", "sleep_score", "steps"):
        if payload.get(key) is not None:
            setattr(row, key,
- [high] ADP-R10 src/wattwise_core/ingestion/adapters/intervals_icu.py:294 â `_build_streams` copies every source stream element verbatim into the canonical typed channel (e.g. `power_w`/`hr_bpm`) with NO numeric normalization â only a None-passthrough. `IntervalsStreamAsbo.data: list[Any]` accepts arbitrary JSON, so a malformed/schema-shifted source payload puts dicts, strings or lists into a canonical SI channel. Reproduced: `IntervalsStreamAsbo(type='watts', data=[{'evil':1},'str',None,[1,2]])` yields a candidate whose `streams.power_w.values == [{'evil':1},'str',None,[1,2]]`. ADP-R10 requires `map` to emit fully NORMALIZED, named, TYPED `stream_channel` rows; this passes a source sub-object through as an un-typed blob. It is also a wrong-but-plausible canonical record forbidden by TIER-R5 / CON-R3 (the FIT/GPX/TCX path correctly coerces every value via `_as_float`, so this adapter is inconsistent and fails the fuzz/degrade contract). | FIX: Coerce each stream element to `float|None` (drop/None non-numeric) before building the channel â e.g. reuse a `_as_float`-style helper as the file-upload decoders do â so a power/hr/cadence channel can never hold a dict/str/list; on a non-numeric element record a typed field-gap (ADP-R12), never pass it through. | BYTES: "values": [None if v is None else v for v in row.data],
- [high] FIL-R1 src/wattwise_core/ingestion/ingest.py:88 â ING-R8/FIL-R1 require that every ingested activity with an original recording file (the file_import tier-1 original) be captured VERBATIM in object storage with a typed activity_file reference to the resolved canonical activity. IngestService.ingest never stores the verbatim bytes and never creates an ActivityFile row; it only persists the source candidate (tier 2) and writes canonical (tier 3). The ActivityFile model and ObjectStore exist but `ActivityFile(...)` is constructed nowhere in the codebase and the object store .put() is never called for activity bytes. Tier-1 capture (the auditable source-of-truth / re-derivation substrate, ING-R8/FIL-R1/FIL-R3) is entirely missing from the ingestion path that owns it. | FIX: On ingesting a file_import activity (and any source that yields an original artifact/full-detail payload), write the verbatim bytes to the ObjectStore and create an ActivityFile(activity_id=resolved, athlete_id, object_ref, format, byte_size, content_hash, source_descriptor_id, fetched_at) row in the same transaction, deduped on (activity_id, source_descriptor_id, content_hash) per FIL-R5. | BYTES: for cand in candidates:
            row = await self._persist_candidate(athlete, descriptor, cand, connection_id, run_id)
            result.candidates_persiste
- [high] ING-R8 src/wattwise_core/ingestion/sync.py:268 â The sync/ingest pipeline acquires source-shaped objects and lands canonical candidates but at no point retains the original recording artifact verbatim (ING-R8: 'the subsystem MUST capture the ORIGINAL artifact VERBATIM and store it in object storage on ingest, creating the typed activity_file reference'). No verbatim-capture step exists in the fetch->map->land flow, so the derive-don't-discard re-derivation contract (FIL-R3) cannot hold â there is nothing to re-derive from without re-fetching. | FIX: Add a verbatim-capture step to the ingest path that, when a run acquires an original file or full-detail payload, stores it byte-for-byte in object storage and records the activity_file reference against the resolved canonical activity, in the landing transaction. | BYTES:     async def _fetch_and_map(
        self,
        adapter: AdapterFetch,
        target: _ConnectionTarget,
        window: SyncWindow,
        fetched_at: _d
- [med] MAP-R4 src/wattwise_core/ingestion/adapters/_map_activity.py:194 â `_sub_sport` lowercases the source token but otherwise returns it verbatim (after filtering only 'generic'/'all'/empty). An arbitrary FIT/GPX/TCX `sub_sport`/`type` token is therefore emitted into the canonical `sub_sport` field (FK into the sub_sport registry, activity.py:71-72) with no mapping to a canonical registry code, contrary to MAP-R4 (vocab->canonical, unknown->other/null) and MAP-R2 (no source-named token in a canonical field). | FIX: Translate the token through a canonical sub_sport registry lookup and return None (or 'other') for unmapped tokens, rather than echoing the lowercased source token. | BYTES:     return token
- [med] MAP-R3 src/wattwise_core/ingestion/adapters/intervals_icu.py:58 â The per-second cumulative `distance` stream from Intervals.icu is a TIME-sampled channel (one value per second carrying cumulative metres), yet it is classed `SampleBasis.DISTANCE`. The file-upload adapter maps the same canonical channel `distance_m` with `SampleBasis.TIME` (_map_activity.py:35, via `_channel(values, SampleBasis.TIME)`). The two adapters thus emit a contradictory canonical `sample_basis` for the identical channel, producing a wrong-but-plausible canonical axis label and breaking source-agnostic consistency for a reader. | FIX: Use `SampleBasis.TIME` for the `distance` stream (it is sampled over time and stores cumulative distance), matching the file-upload adapter's canonical basis for `distance_m`. | BYTES:     "distance": (StreamChannelName.DISTANCE_M, SampleBasis.DISTANCE),
- [med] MAP-R12 src/wattwise_core/ingestion/adapters/intervals_icu.py:278 â `_device_class` infers `gps_watch` provenance purely from the presence of a speed or distance summary value. Distance/speed do not establish that the recording device was a GPS watch (a trainer or phone produces both), so this invents a provenance value the source did not assert. MAP-R12 forbids inventing values; an absent/unestablished canonical field must be left null (`device_class` is nullable, activity.py:90), not defaulted to a plausible-but-fabricated class. | FIX: Return `DeviceClass.UNKNOWN` (or null) when only speed/distance is present; only assign a concrete device class from an actual provenance signal (power_meter/device_watts/trainer flags), which the function already handles for the other branches. | BYTES:     if act.average_speed is not None or act.distance is not None:
        return DeviceClass.GPS_WATCH.value
- [med] MAP-R10 src/wattwise_core/ingestion/ingest.py:171 â In _resolve_activity_id the new candidate's fingerprint is passed as a_fingerprint (line 159/170) but the existing canonical Activity's b_fingerprint is hardcoded `None`. resolve_activity_identity's strong-signal branch (dedup.py:123) requires BOTH fingerprints non-None and equal, so it can NEVER fire against a stored activity â the Activity model carries no fingerprint column and the prior candidates' source_native_id/fingerprint are never compared. MAP-R10 mandates a shared device/file UUID or FIT file fingerprint be SUFFICIENT to match REGARDLESS of the time window; that mandate is unreachable, leaving only the Â±120s/duration window matcher. | FIX: Compare the new candidate's fingerprint against the fingerprints (source_native_id / extracted file/device UUID) of candidates already resolved to each in-window Activity, so a shared strong fingerprint matches independent of the start-time window (MAP-R10). | BYTES: act_start, float(act.elapsed_time_s or 0), act.sport, None,
- [med] CONF-R2 src/wattwise_core/ingestion/ingest.py:303 â _resolve_scalars builds each FieldCandidate WITHOUT a `completeness=` argument, so it defaults to 1.0 (candidate.py:39) for every contributor. _sort_key (dedup.py:58 `-c.completeness`) then never distinguishes any candidates on completeness, making the CONF-R2 tiebreaker-3 (completeness: prefer the candidate with higher coverage for the field, full stream over summary-only) inert â e.g. a full-stream source and a summary-only source contributing the same scalar tie at the completeness step and fall through to the source_descriptor_id tiebreak. | FIX: Derive and set `completeness` on each FieldCandidate (e.g. higher for a stream-backed contribution than a summary-only scalar) so the CONF-R2 step-4 completeness tiebreaker is actually applied. | BYTES:             FieldCandidate(
                value=c.payload[fname],
                trust_tier=_tier_of(c),
                source_descriptor_id=str(c.source_de
- [med] ING-UPS-R1 src/wattwise_core/ingestion/ingest.py:111 â ING-UPS-R1 mandates candidates be upserted by the candidate key using an ATOMIC insert-or-update so re-ingest never duplicates. This path is a check-then-write (SELECT scalar_one_or_none then add), which is exactly the TOCTOU pattern the sanctioned upsert seam (persistence/upsert.py, UPS-R2/R7: 'atomic ... never check-then-write, so there is no race') was built to replace. The uq_source_candidate_candidate_key constraint catches a concurrent duplicate as IntegrityError (fail-closed, not fail-open), but the mandated atomic upsert path is bypassed. | FIX: Route the candidate write through wattwise_core.persistence.upsert.upsert(...) keyed on (athlete_id, source_descriptor_id, source_native_id, gbo_type), matching the same atomic seam the canonical rows are supposed to use. | BYTES:         stmt = select(SourceCandidate).where(
            SourceCandidate.athlete_id == athlete,
            SourceCandidate.source_descriptor_id == descriptor,
- [med] ING-UPS-R5 src/wattwise_core/ingestion/ingest.py:253 â ING-UPS-R5: a write MUST NOT overwrite a higher-trust existing value with a lower-trust new value. _upsert_channel blindly overwrites existing.values with the new candidate's values, and StreamChannel carries no trust/fidelity marker to compare against. Streams are taken from a single _highest_trust(candidates) bundle (line 190), and _highest_trust ranks only on (trust_rank, source_descriptor_id) â it ignores the PRV-R4 tiebreakers (confidence, recency observed_at, completeness). On a re-resolution where the candidate set changes, a lower-trust candidate's stream can clobber a higher-trust stream already written, with no guard and no losing-value retention. | FIX: Before overwriting, compare the incoming stream's fidelity against the persisted channel's (persist a fidelity/winning-candidate pointer on StreamChannel per ING-UPS-R5's lineage-pointing rule); only overwrite when the new value is >= existing trust, and record the displaced value in lineage rather than discarding it. | BYTES:     else:
        existing.values = values
- [med] CLI-R2 src/wattwise_core/ingestion/adapters/_decode_gpx.py:34 â The GPX decoder hands untrusted upload bytes to gpxpy.parse, which (when lxml is installed â it is, for TCX) parses via etree.XML(self.xml, etree.XMLParser(remove_comments=True)) with default entity handling. That parser expands internal entities. Verified end-to-end: a 4-level nested-entity GPX flows through decode_gpx and expands session['title'] to 10000 chars (billion-laughs entity-expansion DoS, trivially scalable to memory exhaustion). The sibling TCX decoder explicitly hardens its parser (resolve_entities=False, no_network=True, load_dtd=False, _decode_tcx.py:56-58); the GPX path does not and cannot, since gpxpy exposes no parser-injection seam. This is a fail-open against malformed/hostile input, contrary to the module's own TIER-R5/CLI-R2 fail-closed claim. | FIX: Before delegating to gpxpy, reject any GPX whose prolog contains a DOCTYPE / entity declaration (fail closed with FileDecodeError), OR parse GPX directly with the same hardened lxml parser used in _decode_tcx (resolve_entities=False, load_dtd=False, no_network=True) instead of gpxpy.parse. | BYTES:         gpx = gpxpy.parse(data.decode("utf-8", errors="strict"))
- [med] PRV-R5 src/wattwise_core/ingestion/ingest.py:314 â resolve_field is called without dispute_tolerance, so ResolvedField.disputed is always False, and _apply_activity_scalars writes no Coverage at all for the resolved activity. PRV-R5 requires the engine to set coverage disputed:true iff â¥2 candidates materially disagree and record a non-blocking conflict lineage note. The Coverage model has the disputed field and dedup.resolve_field computes it, but the canonical write never threads tolerance through nor persists disputed/conflict, so material multi-source disagreement is silently dropped (the winner is still correct, but the disagreement is never surfaced to coverage/lineage). | FIX: Pass a configured dispute_tolerance into resolve_field for numeric scalars and, when ResolvedField.disputed is True, write the field's Coverage with disputed=True plus a conflict lineage note, rather than discarding the disputed signal. | BYTES:         winner = resolve_field(contributors)
        if winner is not None:
            resolved[fname] = winner.value
- [med] ING-R7 src/wattwise_core/ingestion/adapters/_decode_gpx.py:34 â Untrusted GPX upload bytes are parsed via gpxpy.parse with no XXE hardening. gpxpy parses XML through lxml's XMLParser configured only with remove_comments=True (verified in gpxpy/parser.py: root = mod_etree.XML(self.xml, mod_etree.XMLParser(remove_comments=True))), so external-entity resolution is left at lxml's default. The sibling TCX decoder (_decode_tcx.py lines 56-58) explicitly sets resolve_entities=False, no_network=True, dtd_validation=False, load_dtd=False; the GPX path has none. The GPX path's safety is incidental: it is closed only because the lockfile pins lxml 6.1.1 (default resolve_entities='internal'), but pyproject declares lxml>=5.3, which permits a resolved lxml<6.1 where resolve_entities defaults to True -> a malicious GPX carrying a DOCTYPE + external entity (the file passes detect_format because it still contains '<gpx') achieves local-file disclosure (network is blocked by lxml's no_network=True default). This is a fail-open-by-default for untrusted source text, contradicting this module's own 'fails closed' docstring (lines 6-7) and the ingestion fail-closed posture. | FIX: Do not rely on gpxpy's incidental parser config. Either (a) pre-reject any GPX whose bytes contain a DOCTYPE/<!ENTITY before calling gpxpy and fail closed with FileDecodeError, or (b) parse the GPX yourself with a hardened lxml XMLParser(resolve_entities=False, no_network=True, load_dtd=False, resolve_entities is False) mirroring _decode_tcx._parse_xml and feed the cleaned tree to gpxpy, and/or pin lxml>=6.1 in pyproject so the safe default is guaranteed. Add a test that a GPX with an external/local-file entity is rejected, matching the XXE guarantee asserted for TCX. | BYTES: gpx = gpxpy.parse(data.decode("utf-8", errors="strict"))
- [med] ADP-R10 src/wattwise_core/ingestion/adapters/intervals_icu.py:310 â MAP-R4 (= ADP-R10) requires the map to be the single place source vocabulary is translated to canonical terms, emitting NORMALIZED canonical values. The Intervals adapter passes the source `sub_type` token straight through into the canonical `sub_sport` field with no normalization, whereas the file_upload adapter normalizes its sub_sport (lowercases, drops 'generic'/'all', _map_activity._sub_sport). A raw source enum value therefore reaches a canonical field from one source but not the other â a source-shaped passthrough and a cross-adapter inconsistency. | FIX: Normalize Intervals `sub_type` through the same canonical sub_sport normalization used by _map_activity._sub_sport (or a shared helper) so the canonical sub_sport value is source-neutral and consistent across adapters. | BYTES:         "sub_sport": act.sub_type,
- [med] ING-UPS-R5 src/wattwise_core/ingestion/ingest.py:190 â PRV-R6/ING-UPS-R5 require higher-fidelity streams to win per channel and a write to never overwrite a higher-trust existing value with a lower-trust one. Streams are taken wholesale from a single _highest_trust candidate's payload, not resolved per channel across candidates. If the highest-trust candidate lacks a given channel that a lower-trust candidate provides, that channel is dropped (not filled from the lower-trust contributor); conversely _upsert_channel does `existing.values = values` unconditionally, so it relies entirely on _highest_trust never regressing rather than on a per-channel trust comparison. This is per-record rather than per-channel field resolution (CONF-R3). | FIX: Resolve streams per channel across all retained candidates using the trust order (raw_stream > device_computed > ... ), and in _upsert_channel only overwrite when the incoming channel's trust is >= the stored channel's trust, recording the displaced value in lineage. | BYTES:         best = _highest_trust(candidates)
        streams = cast("dict[str, Any]", best.payload.get("streams") or {})
        laps = cast("list[dict[str, Any]]""
}