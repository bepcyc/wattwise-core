## Agent: 70 findings (35 high, 29 med, 6 low)
- [high] GRAPH-R5 src/wattwise_core/agent/graph.py:371 â `finalize` sets `AWAITING_APPROVAL`. GRAPH-R5/OUTCOME-R1 are explicit: `finalize` emits exactly `completed | degraded | budget_exceeded`; `awaiting_approval` is 'the one terminal that does NOT reach `finalize`' â `interrupt_gate` PAUSES and YIELDS it. Here `interrupt_gate` (_interrupt_gate) only stamps a flag and never pauses/yields; the actual pause is a build-time `interrupt_before=['finalize']` flag, and the status is wrongly assigned at finalize. The status-emitting node is wrong and the pause is not emitted at interrupt_gate. | FIX: Emit `awaiting_approval` at `interrupt_gate` via a langgraph durable interrupt (carrying thread_id + interrupt_id + grounded plan) so the run PAUSES there; restrict `_finalize` to only set completed/degraded/budget_exceeded. | BYTES: status = RunStatus.AWAITING_APPROVAL
- [high] GROUND-R9 src/wattwise_core/agent/graph.py:336 â `interrupt_gate` conflates the grounder's ABSTAIN with human approval, then `_finalize` (line 370) turns ABSTAIN into `AWAITING_APPROVAL`. GROUND-R9 / REFLECT-R2a cross-map require `abstain` â `interrupt_gate`/`finalize` emitting a typed `degraded` deliverable, never `awaiting_approval` (which is reserved for an approval-gated PLAN at interrupt, CKPT-R5). An abstaining run thus self-mislabels its terminal status and never ships the required degraded/limitation outcome. | FIX: Map GroundDecision.ABSTAIN to a `degraded` finalize with a coverage caveat (GROUND-R6); derive approval-required from product plan-approval policy on a training PLAN deliverable (CKPT-R5/COACH-R4), not from the grounding decision. | BYTES: approval_required = decision is GroundDecision.ABSTAIN
- [high] GRAPH-R5 src/wattwise_core/agent/graph.py:498 â No configured node-visit ceiling is wired. GRAPH-R5 requires that exceeding the node-visit ceiling 'MUST route to `finalize` with a `degraded` status rather than erroring or looping'. With langgraph's default `recursion_limit`, an over-long run raises `GraphRecursionError` (an error), violating the graceful-degraded mandate (OUTCOME-R3). The bounded counters cap the recovery cycles but there is no overall visit ceiling routed to a degraded finalize. | FIX: Add a configured node-visit ceiling tracked in state and a conditional edge that routes to `finalize` with `RunStatus.DEGRADED` on exhaustion; never let the run raise GraphRecursionError. | BYTES: return builder.compile(checkpointer=checkpointer, interrupt_before=interrupt_before)
- [high] PLAN-R3 src/wattwise_core/agent/tools.py:178 â The MCP tool registry keys (`training_load_window`, `critical_power_fit`, `readiness_hrv`, `activity_load_metrics`, `activity_decoupling`) are DIFFERENT from the planner capability-registry keys in capabilities.py (`weekly_load`, `critical_power`, `hrv`, `load_metrics`, `decoupling`). PLAN-R3/TOOL-R1 require the capability registry be 'the SINGLE authoritative list ... shared VERBATIM with the MCP tool layer'. These are two divergent key spaces, not one registry surfaced twice; a planner-selected `weekly_load` has no matching tool and vice-versa. | FIX: Derive the MCP tools from the single CAPABILITIES tuple (capabilities.py) so tool keys == registry keys verbatim; remove the parallel hand-written `_TOOLS` key set. | BYTES: Tool("training_load_window", "Recent training-load state (CTL/ATL/form).", _pmc),
- [high] PLAN-R5 src/wattwise_core/agent/capabilities.py:197 â `gather` scopes to the injected `athlete_id` but never inspects requests for an athlete/scope override and never records the AGT-OBS-R5 injection/anomaly event PLAN-R5 mandates ('If the plan references any athlete other than the authenticated one, `gather` MUST ignore the override and ... MUST record an injection/anomaly event'). Ignoring-by-omission silently drops the detection+log obligation; there is no anomaly emission path here or in graph._make_gather. RetrievalRequest.params is an open dict that could carry an athlete key with no detection. | FIX: Detect any athlete/scope-shaped key in req.params, ignore it, and emit the AGT-OBS-R5a anomaly event (attempt, ignored override, authenticated scope used) correlated to the run trace. | BYTES: async def gather(
    svc: AnalyticsService,
    athlete_id: str,
    requests: list[RetrievalRequest],
) -> dict[str, Any]:
- [high] PLAN-R3 src/wattwise_core/agent/tools.py:177 â The MCP tool layer defines its OWN capability-key vocabulary (training_load_window, critical_power_fit, readiness_hrv, activity_load_metrics, activity_decoupling) which does NOT match the single capability registry keys in capabilities.py (weekly_load, critical_power, load_metrics, hrv, decoupling, trimp). PLAN-R3/TOOL-R1 require ONE registry shared VERBATIM with the MCP tool layer, each entry 1:1, two surfaces over one registry. These are two divergent key sets; the planner can plan keys the tools don't expose and vice versa â not one registry. tools.py never imports CAPABILITIES/CAPABILITY_BY_KEY. | FIX: Derive the MCP tools from the single CAPABILITIES tuple in capabilities.py (one source of truth); use identical capability keys and the same _RESOLVERS dispatch so the planner's gather and the MCP tools resolve the SAME registry entry verbatim. | BYTES: _TOOLS: tuple[Tool, ...] = (
    Tool("training_load_window", ...), Tool("power_curve", ...), Tool("critical_power_fit", ...), Tool("readiness_hrv", ...), Tool(
- [high] STATE-R4 src/wattwise_core/agent/contracts.py:67 â The immutable input fields athlete_id and idempotency_key carry NO reducer (no Annotated[..., write_once_reducer]), unlike the accumulating fields which use _append/_monotonic/etc. STATE-R4 mandates these be write-once and that the REDUCER MUST RAISE on a changed value. In LangGraph a channel with no reducer is last-value-wins (silently overwritable), so a node (or model/tool-derived update) can overwrite athlete_id â directly defeating AGT-SEC-R1's structural identity guarantee. No write-once reducer exists. | FIX: Annotate athlete_id and idempotency_key with a write-once reducer that raises ValueError when the incoming value differs from the already-set value (analogous to _monotonic), so any overwrite attempt is rejected at the reducer level. | BYTES:     athlete_id: str
    trigger: Trigger
    request_text: str | None
    briefing_screen: str | None
    locale: str
    idempotency_key: str
- [high] MEM-R3 src/wattwise_core/agent/memory.py:69 â Durable athlete memory is registered on the CANONICAL ORM Base (from wattwise_core.persistence.base, whose MetaData is the canonical GBO store) and its athlete_id FK targets canonical 'athlete.athlete_id' (line 81). MEM-R3 requires memory live in the DEDICATED agent-state store (the AgentStateBase used by state_store.py), NEVER the canonical GBO store. state_store.py's own docstring asserts the store-separation 'holds by construction' because agent tables are on AgentStateBase; memory_item breaks that by sitting in canonical metadata. | FIX: Move MemoryItem onto the agent-state store: declare it on AgentStateBase (agent_state metadata) with the agent_ table prefix and the agent-state migration, not on canonical Base; drop the canonical athlete FK or replace with an agent-state-side athlete scope column. | BYTES: class MemoryItem(Base, TimestampMixin):
- [high] OUTCOME-R1 src/wattwise_core/agent/graph.py:370 â _finalize maps a grounding ABSTAIN decision to RunStatus.AWAITING_APPROVAL. Per OUTCOME-R1 and the REFLECT-R2a cross-map, ground's `abstain` MUST yield a `degraded` (limitation) deliverable, while `awaiting_approval` is emitted ONLY for an approval-gated plan paused at interrupt_gate (CKPT-R5). Conflating abstain (a grounding failure -> degraded) with awaiting_approval mislabels the terminal status, breaks the closed status union semantics, and would make an un-answerable run look like a plan awaiting approval. | FIX: Map ABSTAIN to RunStatus.DEGRADED with a coverage_caveat (GROUND-R6/OUTCOME-R4). Reserve AWAITING_APPROVAL strictly for approval-gated training-plan deliverables at interrupt_gate, gated by product approval policy â never by a grounder abstain. | BYTES:     if decision is GroundDecision.ABSTAIN or _approval_required(state):
        status = RunStatus.AWAITING_APPROVAL
- [high] CKPT-R5 src/wattwise_core/agent/graph.py:336 â interrupt_gate sets approval_required from a grounder ABSTAIN decision. CKPT-R5 defines the HITL interrupt as triggered when the DELIVERABLE is a training PLAN that product policy marks as requiring approval â not when grounding abstains. The node also generates no unique interrupt_id, persists no proposed grounded plan, and yields no awaiting_approval payload, so the durable-interrupt contract (grounded plan + thread_id + interrupt_id) is absent. The approval trigger is semantically wrong and the interrupt machinery is unimplemented. | FIX: Drive approval_required from the deliverable type + product approval policy (plan-requiring-approval), not from abstain. At the interrupt, mint a unique interrupt_id, persist the grounded plan to the agent-state store, and surface an awaiting_approval payload carrying {grounded_plan, thread_id, interrupt_id} (CKPT-R5/-R6). | BYTES:     approval_required = decision is GroundDecision.ABSTAIN
- [high] REFLECT-R2 src/wattwise_core/agent/graph.py:249 â The reflect node only increments reflection_count; it produces NO structured decision over the closed enum `replan | answer_with_caveat | give_up_gracefully` (REFLECT-R2). The enum is defined nowhere in the codebase (grep finds no answer_with_caveat/give_up_gracefully). Routing is decided purely by deterministic gap-count + budget in _route_after_assess/_route_after_reflect. REFLECT-R2 mandates reflect emit a structured Â§6 verdict over that closed enum, considering coverage_gaps, with a replan identifying which capability requests to add/widen. | FIX: Define the closed ReflectDecision verdict schema (replan|answer_with_caveat|give_up_gracefully) and obtain it via run_structured() in _reflect; route on the verdict; for replan, carry the capability requests to add/widen. | BYTES: def _reflect(state: AgentState) -> dict[str, Any]:
    ... count = state.get("reflection_count", 0) + 1 ...
- [high] STRUCT-R1 src/wattwise_core/agent/graph.py:170 â The retrieval plan, the coverage judgement, intent classification, and the reflect decision are never produced as provider-enforced structured outputs in the graph. run_structured (structured.py) exists but is never called by graph.py; the Planner/CoverageAssessor seams return raw Sequence[RetrievalRequest]/set[str] with no schema-constrained verdict and no closed-schema validation. STRUCT-R1 requires EVERY verdict (intent, retrieval plan, coverage judgement, reflection decision, grounding span extraction, plan structure, readiness call) be a provider-enforced structured output. | FIX: Define closed pydantic verdict schemas for intent, retrieval plan, and coverage judgement and obtain them through run_structured()/model.structured() in the respective nodes; validate before consuming (STRUCT-R2/-R3). | BYTES: def _make_plan_retrieval(svc: AgentServices) -> GraphNode:
    async def plan_retrieval(state: AgentState) -> dict[str, Any]:
        ... requests = await svc.p
- [high] COST-R2 src/wattwise_core/agent/graph.py:156 â ingest_request contains NO cost-admission gate call-site and finalize contains NO cost-settle gate call-site. COST-R2/-R3 require the reserve/admission gate call-site at ingest_request and the settle gate at finalize to EXIST IN OSS (degenerating to a non-monetary no-op / local guard), so a commercial metered resolver plugs in without an agent-graph change. Neither gate, nor any optional local guard (max steps / wall-clock / max-tokens / max-tool-iterations), is wired. budget_exceeded is consequently never emittable by _finalize (AGT-SEC-R3/COST-R4 require the budget_exceeded node-boundary stop variant in OSS). | FIX: Add an entitlement/cost-admission resolver seam called in ingest_request and a cost-settle seam called in finalize (OSS default: non-monetary no-op enforcing local guards). On a per-run-ceiling breach at a node boundary, finalize with RunStatus.BUDGET_EXCEEDED carrying the best grounded partial + cap (COST-R4). | BYTES: def _ingest_request(state: AgentState) -> dict[str, Any]:
    _athlete_id(state)
    text = state.get("request_text")
    ... return {"messages": messages}
- [high] GRAPH-R5 src/wattwise_core/agent/graph.py:353 â There is no configured node-visit ceiling that routes to finalize with degraded on overflow. GRAPH-R5 requires termination within a CONFIGURED node-visit ceiling and that exceeding the ceiling routes to finalize with a degraded status. Termination relies solely on hardcoded MAX_REFLECTIONS/MAX_REDRAFTS module constants (no config); no independent node-visit/step ceiling exists, and no config knob (settings.py has no agent guard knobs) backs any of these bounds. | FIX: Add a configured node-visit/step ceiling (and load the reflection/redraft maxima from config per REFLECT-R1's 'configured constant'); on exceeding the ceiling route to finalize emitting RunStatus.DEGRADED (never error, never budget_exceeded). | BYTES: def _finalize(state: AgentState) -> dict[str, Any]:
    ... budget_spent = reflections >= MAX_REFLECTIONS or redrafts >= MAX_REDRAFTS
- [high] OUTCOME-R1 src/wattwise_core/agent/graph.py:370 â The grounder's `abstain` decision is mapped to the `awaiting_approval` terminal status. The REFLECT-R2a cross-map and GROUND-R9 are explicit: `ground` -> `abstain` MUST finalize as `degraded` with a limitation deliverable ("cannot ground enough to answer"). `awaiting_approval` is reserved for an approval-gated PLAN paused at a durable interrupt (CKPT-R5). Here abstain (a fail-closed grounding outcome) is silently reclassified as awaiting-approval, so a run that could not be grounded is reported as a plan awaiting a human decision â an incorrect, non-degraded terminal status that also never carries the required coverage_caveat (OUTCOME-R4). | FIX: Map GroundDecision.ABSTAIN to RunStatus.DEGRADED (with a typed coverage_caveat); reserve AWAITING_APPROVAL strictly for a plan-type deliverable that policy marks as requiring approval and that paused at interrupt_gate (CKPT-R5). | BYTES: if decision is GroundDecision.ABSTAIN or _approval_required(state):
        status = RunStatus.AWAITING_APPROVAL
- [high] MEM-R3 src/wattwise_core/agent/memory.py:69 â Durable athlete memory is persisted on the CANONICAL ORM base (`Base` from persistence.base, the GBO store) and its FK is `fk_uuid_column("athlete.athlete_id")` (line 81) referencing a canonical table. MEM-R3 requires memory to live in the DEDICATED agent-state store (the separate `AgentStateBase` metadata used by state_store.py), NEVER the canonical GBO store. By registering on canonical `Base`, the memory table joins the canonical metadata â the exact store-separation the agent-state store was built to prevent (ARCH-R13/CKPT-R1). It also lands under the canonical write credential rather than the agent-state credential (DEPLOY-R4). | FIX: Re-base MemoryItem on `wattwise_core.agent.state_store.AgentStateBase` (the agent-state metadata), give it the `agent_` table prefix, and replace the canonical-table FK with an athlete_id column not foreign-keyed into the canonical `athlete` table (defence-in-depth scoping like AgentCheckpoint), so memory cannot appear in canonical metadata. | BYTES: class MemoryItem(Base, TimestampMixin):
- [high] GRAPH-R5 src/wattwise_core/agent/graph.py:353 â There is no configured node-visit ceiling and no `budget_exceeded`/local-guard path anywhere in the graph. GRAPH-R5 requires the graph to terminate within a configured node-visit ceiling and route a ceiling breach to `finalize` with `degraded`; AGT-SEC-R3/AGT-ENT-R4/COST-R1 require the OSS default to enforce an optional local guard (max steps / wall-clock / max-tokens / max-tool-iterations) and the OSS `budget_exceeded` outcome variant + node-boundary stop to exist so the commercial metered resolver can plug in without an agent-graph change. `_finalize` only ever returns COMPLETED/DEGRADED/AWAITING_APPROVAL; RunStatus.BUDGET_EXCEEDED is defined (contracts.py:27) but never emitted, and no admission/settle gate-site exists at ingest_request or finalize. The cost-admission gate of GRAPH-R2 node1 and the cost-settle gate of node9 are absent. | FIX: Add a node-visit/step counter and a cost-admission gate at ingest_request plus a settle gate at finalize (no-ops under OSS default but present as seams); route a node-visit-ceiling breach to finalize->DEGRADED and a (commercial) per-run-ceiling breach to a node-boundary stop emitting BUDGET_EXCEEDED. | BYTES: def _finalize(state: AgentState) -> dict[str, Any]:
- [high] MEM-R3 src/wattwise_core/agent/memory.py:69 â Durable athlete memory is registered on the CANONICAL GBO metadata, not the dedicated agent-state store. MEM-R3 mandates: "Memory MUST be in the dedicated agent-state store (never the canonical GBO store)" and MEM-R4: the OSS engine memory lives behind the relational agent-state store. The checkpoint store deliberately defines a separate `AgentStateBase` MetaData (state_store.py:57) precisely so agent state never shares canonical metadata/schema/write-credential (ARCH-R13/R29). `MemoryItem` imports `from wattwise_core.persistence.base import Base` (memory.py:31) â the "Declarative base for every canonical ORM model" â so `memory_item` lands in `Base.metadata` (the GBO store). The store-separation test confirms the leak by EXCLUDING it: tests/unit/test_checkpoint.py:217 asserts `agent_state == {"agent_thread", "agent_checkpoint", "agent_write"}` (no `memory_item`), and the GDPR erasure test builds memory under the canonical schema. The migration docstring (0003_agent_memory.py) even claims it creates the "dedicated agent-state `memory_item` table", contradicting the ORM. Agent durable memory is co-located in the canonical store, violating the structural separation MEM-R3/MEM-R4/ARCH-R13 require. | FIX: Re-base `MemoryItem` on `AgentStateBase` (from wattwise_core.agent.state_store), mirroring AgentThread/AgentCheckpoint/AgentWrite, so `memory_item` is registered on the agent-state MetaData and gets its own write credential. Move its creation into the agent-state migration (0002_agent_state) rather than a canonical-schema revision, and update the store-separation test to include `memory_item` in the agent_state table set (and assert it is NOT in canonical metadata). | BYTES: class MemoryItem(Base, TimestampMixin):
- [high] STATE-R4 src/wattwise_core/agent/contracts.py:67 â The immutable write-once inputs `athlete_id` (line 67) and `idempotency_key` (line 72) are plain TypedDict fields with NO `Annotated[..., reducer]`. STATE-R4 requires: "Any node attempting to overwrite them MUST be rejected at the reducer level (the reducer MUST raise on a changed value)." LangGraph assigns a non-Annotated state field the default `LastValue` channel (confirmed `.venv/.../langgraph/graph/state.py:1857` `fallback: LastValue = LastValue(annotation)`), which SILENTLY overwrites on the next write and never raises. So a model/tool output (or a buggy node) that emits a different `athlete_id`/`idempotency_key` is accepted rather than rejected â a fail-open identity-mutation path. The docstring at line 61 claims "write-once (STATE-R4)" but nothing enforces it; `_athlete_id()` in graph.py only checks presence, not immutability. No test asserts a changed-identity write raises (tests/unit/test_graph.py covers only missing identity). | FIX: Add a write-once reducer, e.g. `def _write_once(left, right): if left is not None and right != left: raise ValueError('write-once field changed (STATE-R4)'); return right if left is None else left`, and annotate `athlete_id: Annotated[str, _write_once]` and `idempotency_key: Annotated[str, _write_once]`. Add a property/unit test that a second write of a different value raises. | BYTES:     athlete_id: str
    trigger: Trigger
    request_text: str | None
    briefing_screen: str | None
    locale: str
    idempotency_key: str
- [high] MEM-R3 src/wattwise_core/agent/memory.py:69 â Durable athlete memory is declared on the CANONICAL `Base` (imported line 31 `from wattwise_core.persistence.base import Base`; `Base` is documented in persistence/base.py:33 as "Declarative base for every canonical ORM model"). MEM-R3 mandates: "Memory MUST be in the dedicated agent-state store (never the canonical GBO store)". The agent-state store module (state_store.py) deliberately introduces a SEPARATE `AgentStateBase` metadata precisely so agent state can never appear in canonical metadata (its docstring: store-separation "holds by construction"). `MemoryItem` bypasses that and lands the memory table in the canonical GBO metadata, structurally violating store separation and the doc-10 ARCH-R13 inventory that places memory in the agent-state category. | FIX: Re-base `MemoryItem` on `AgentStateBase` from wattwise_core.agent.state_store (give it its own UTC-timestamp columns or an agent-state TimestampMixin), so the memory table registers on the agent-state metadata, not the canonical `Base`. Add a store-separation assertion that `memory_item` is absent from canonical metadata. | BYTES: class MemoryItem(Base, TimestampMixin):
- [high] STATE-R6 src/wattwise_core/agent/contracts.py:41 â The `retrieved` keyed-merge reducer performs an UNBOUNDED dict merge. STATE-R6 requires: "The reducer for `retrieved` MUST enforce a maximum record count and a maximum serialized size; on overflow it MUST drop lowest-relevance records (relevance assigned by `gather`) and record the truncation in `coverage_gaps`." There is no record-count cap, no serialized-size cap, no lowest-relevance drop, and no truncation note written to `coverage_gaps` anywhere (grep for STATE-R6/max_record/relevance/truncate over agent/* returns nothing). State serialization is therefore unbounded, breaking STATE-R6 and the checkpointer size-budget guarantee. | FIX: Make the `retrieved` reducer bound-aware: cap record count and serialized size, drop lowest-relevance records (relevance from `gather`), and surface the drop into `coverage_gaps`. Because a binop reducer only sees `(left, right)` it cannot reach `coverage_gaps`; move the bound into the `gather` node (return both the trimmed `retrieved` and the `coverage_gaps` truncation marker) or carry relevance on each record so the reducer can rank+trim. | BYTES: def _keyed_merge(left: dict[str, Any], right: dict[str, Any]) -> dict[str, Any]:
    """retrieved reducer: merge by canonical record id; later replaces earlier 
- [high] OUTCOME-R3 /home/bepcyc/dev/my/prod/wattwise/wattwise-core/src/wattwise_core/agent/graph.py:370 â A grounding abstain (insufficient grounded data to answer) is finalized as AWAITING_APPROVAL, not degraded. REFLECT-R2a cross-map is explicit: ground/abstain -> finalize with limitation deliverable -> OUTCOME-R1 status degraded. awaiting_approval is reserved solely for an approval-gated plan paused at a durable interrupt (CKPT-R5/OUTCOME-R1), and OUTCOME-R3 requires abstain to be a first-class graceful degraded outcome. This breaks the Phase-1 weekly-digest path (COACH-R1 #1) whose documented contract is to abstain visibly as degraded+caveat when canonical inputs are missing (deliverables.py Digest docstring): such a digest would emit awaiting_approval, a non-terminal HITL status with no plan/interrupt_id. Root miswiring is _interrupt_gate approval_required = decision is GroundDecision.ABSTAIN at graph.py:336. | FIX: Map a grounding abstain to RunStatus.DEGRADED (coverage_caveat already built at line 377). Reserve AWAITING_APPROVAL solely for genuine plan-approval interrupts (not shipped in Phase-1); stop treating abstain as approval in _interrupt_gate (graph.py:336). | BYTES: if decision is GroundDecision.ABSTAIN or _approval_required(state):
        status = RunStatus.AWAITING_APPROVAL
- [high] EVAL-R1 justfile:155 â The CI grounding/abstention/injection gate invokes `python -m wattwise_core.eval run`, but the eval package has NO `__main__.py` (and no `cli`/`run`/`record`/`update-baseline` entrypoint anywhere in src/wattwise_core/eval/). The module-execution will raise `No module named wattwise_core.eval.__main__`, so `just eval` (and the `.github/workflows/ci.yml` line 115 `agent-eval` job) cannot run the suite at all. EVAL-R1's 'CI MUST fail the build when suite metrics fall below thresholds' is unmet: the gate command crashes before ever computing a metric, and no scorecard artifact (EVAL-R9) is produced. | FIX: Add `src/wattwise_core/eval/__main__.py` (or a console-script) implementing the `run --mode=recorded --scorecard=...`, `record`, and `update-baseline` subcommands the justfile/CI invoke, calling `run_suite` over all suites and writing the JSON scorecard; or change the justfile to call an existing entrypoint. | BYTES: {{uv}} python -m {{package}}.eval run --mode=recorded --scorecard=reports/eval-scorecard.json
- [high] EVAL-R4 src/wattwise_core/eval/runner.py:214 â The CI grounding gate (QA-EVAL-R6: faithfulness>=99%, zero fabricated) is computed against a SECOND, re-implemented grounder (`_ground_case` / `_classify_claim`, runner.py L214-290), NOT the production `wattwise_core.agent.grounding.ground`. runner.py imports only `Claim, ClaimKind` from contracts and `FakeModel` from model; it never imports `ground`. agent/grounding.py depends only on agent.contracts (the published seam), so it IS importable across the layer boundary â the docstring's claim that 'graph/grounding sibling modules are not importable' is false for grounding. Result: the deterministic gate certifies a parallel copy that can silently diverge from production (e.g. production `_decide` returns CONTRADICTED->REPLAN with the strongest penalty; the runner has no GroundDecision/verdict and merely scrubs). 'The grounding stage MUST scrub 100% of planted hallucinations' is asserted of code that does not ship to the athlete. | FIX: Drive the eval cases through the production `ground(draft, claims, evidence, allow_urls)` and grade its `GroundingResult`/`GroundDecision`, deleting the runner's private `_ground_case`/`_classify_claim` copy, so the gate exercises the production grounding identity path. | BYTES: def _ground_case(
    case: dict[str, Any],
    auth_id: str | None,
    claims: tuple[Claim, ...],
- [high] EVAL-R6 src/wattwise_core/eval/runner.py:312 â The injection 'neutralized' verdict is tautological. `_injection_facts` builds `eff_scope` ONLY from the dataset's `authenticated.allowed_capabilities` (L331) and asserts it equals `expected.scope_unchanged`; `injected_athlete_id`/`injected_capabilities`/`injected_urls` are never read into any effective identity/scope/tooling that a production planner/gather (PLAN-R5, AGT-SEC-R1) would compute. The probe cannot alter scope because nothing in the harness ever consults the injected values â identity/scope/tooling-unchanged is guaranteed by construction, not demonstrated. INJECT-R4/EVAL-R6 require probes that 'demonstrably FAIL to alter identity, scope, tooling' against the engine; this proves only the stub's own isolation, so a real scope-override regression in production would pass the gate. | FIX: Route injection cases through the real plan->gather scope-resolution (or the production scope-enforcement seam) so the effective scope is derived from authenticated context vs. the injected override, and assert the injected athlete_id/capabilities are demonstrably dropped by that code, emitting the AGT-OBS-R5a anomaly event. | BYTES: def _injection_facts(
    case: dict[str, Any],
    auth_id: str | None,
    non_canonical: set[str],
    authenticated: dict[str, Any] | None,
- [high] EVAL-R7 src/wattwise_core/eval/runner.py:382 â The termination suite EVAL-R7 (mandated: two distinct fixtures exercising the `reflection_count` coverage bound AND the `redraft_count` re-draft bound, 'gated in QA-EVAL-R6, not left as a roadmap item') is entirely absent. The eval package has only grounding/abstention/injection datasets and graders; there is no `reflection_count`/`redraft_count`/termination dataset, grader, or RunnerOutcome field (`rg` for 'termination|reflection_count|redraft_count' in src/wattwise_core/eval returns nothing). REFLECT-R4's required assertion that a perpetually-insufficient/perpetually-failing-to-ground fixture terminates at its bound without an unbounded loop is not CI-gated here. | FIX: Add a termination dataset + grader exercising both bounded counters against the production graph/reflect/ground cycle, catalogued and gated like the other suites. | BYTES: _SUITE_GRADERS: dict[str, frozenset[str]] = {
    "grounding": frozenset({"grounding", "schema"}),
    "abstention": frozenset({"abstention", "schema"}),
    "i
- [high] EVAL-R3 src/wattwise_core/eval/runner.py:408 â The mandated intent/retrieval-plan accuracy suite with the >=0.9 precision/recall floor (EVAL-R3, bound in QA-EVAL-R6) is missing entirely. There is no intent/plan dataset, no precision/recall grader, and `run_suite` only knows grounding/abstention/injection. The planner (PLAN-R*) is never scored, so the >=0.9 CI gate the spec requires does not exist. | FIX: Add a labeled intent/retrieval-plan dataset and a precision/recall grader over the production planner's emitted capability requests, gating CI at >=0.9. | BYTES: async def run_suite(name: str, *, mode: EvalMode = EvalMode.RECORDED) -> Scorecard:
- [high] EVAL-R2a src/wattwise_core/eval/runner.py:196 â The memory-non-substitution obligation (EVAL-R2a / MEM-R3: 'MUST assert the agent's answer cites the LIVE canonical value, NOT the memory-stored value') is not actually tested. The runner never reads the dataset's `stale_memory` (grounding.json L97 `"stale_memory": {"ctl": 55.0}`) nor `must_cite_live_not_memory` (L110); memory is never injected into the pipeline at all. The test `test_grounding_memory_non_substitution_cites_live_value` (tests/unit/test_eval.py L83-94) only asserts `live == expectation["live_value"]` and `live_value != memory_value` â pure assertions about the fixture's own JSON literals. A pipeline that DID substitute the stale memory value would still pass, so the MEM-R3 'never substitutes for a live canonical number' guarantee is unproven. | FIX: Feed the stale memory value into the production pipeline as a competing candidate and assert the deterministic grounder surfaces the live canonical value (72.4), scrubbing/ignoring the memory value (55.0). | BYTES: model = FakeModel(prose=str(case.get("draft_prose", "")))
- [high] ARCH-R13 src/wattwise_core/agent/memory.py:69 â Durable athlete memory is mapped on the CANONICAL declarative Base (imported at line 31: `from wattwise_core.persistence.base import Base`), whose docstring states it is the base 'for every canonical ORM model'. The agent-state store (checkpoints/threads/writes in state_store.py) correctly uses a SEPARATE `AgentStateBase`, but `MemoryItem` lands in the canonical GBO metadata. The lens explicitly requires AgentStateBase, not canonical Base, and to flag MemoryItem if on canonical Base. | FIX: Register MemoryItem on AgentStateBase (the dedicated agent-state MetaData in state_store.py), not the canonical `Base`. Memory is an agent-state-store category (MEM-R3/ARCH-R13) and MUST NOT share metadata/schema/write-credential with canonical master data; the ARCH-R29 store-separation test should fail on this row. | BYTES: class MemoryItem(Base, TimestampMixin):
- [high] MEM-R3 src/wattwise_core/agent/memory.py:69 â MEM-R3 mandates memory be 'in the dedicated agent-state store (never the canonical GBO store)'. Placing MemoryItem on the canonical `Base` writes durable memory into the canonical store and couples its erasure/retention/write-role to canonical data, breaking the structural separation MEM-R3/CKPT-R8 depend on (per-athlete erasure must remove memory rows together with checkpoints in the agent-state store, not from GBO). | FIX: Move MemoryItem onto AgentStateBase so memory persists in the agent-state schema with its own write role and is purged/erased alongside checkpoints, never from the canonical GBO store. | BYTES: class MemoryItem(Base, TimestampMixin):
- [high] PLAN-R3 src/wattwise_core/agent/tools.py:528 â The MCP tool layer declares its OWN capability keys (`training_load_window`, `power_curve`, `critical_power_fit`, `readiness_hrv`, `activity_load_metrics`, `activity_decoupling`) that are DISJOINT from the planner/gather registry keys in capabilities.py CAPABILITIES (`weekly_load`, `critical_power`, `power_curve`, `load_metrics`, `hrv`, `decoupling`, `trimp`). PLAN-R3 requires the registry presented to the planner be 'shared VERBATIM with the MCP tool layer'; TOOL-R1 requires each tool be a wrapper over 'the SAME entry planner selects'. These are two parallel catalogs, not one registry â a planner-selected key (e.g. `weekly_load`) has no matching tool, and a tool key (e.g. `training_load_window`) is not in the registry the planner is constrained to. | FIX: Build the MCP ToolRegistry FROM the single CAPABILITIES registry (capabilities.py) so tool.capability == Capability.key verbatim and both surfaces resolve the identical registry entry. Do not hand-author a second key set in tools.py. | BYTES: _TOOLS: tuple[Tool, ...] = (
    Tool("training_load_window", ...)
- [high] OUTCOME-R1 src/wattwise_core/agent/graph.py:370 â A grounding `abstain` decision is mapped to terminal status AWAITING_APPROVAL. Per OUTCOME-R1 and the REFLECT-R2a cross-map, `ground` -> `abstain` MUST finalize as `degraded` with a limitation deliverable; `awaiting_approval` is reserved for an approval-gated PLAN paused at an interrupt (CKPT-R5). This conflates 'cannot ground enough to answer' (degraded) with 'plan awaiting human approval', so an unanswerable run is reported as awaiting_approval and never produces the truthful degraded/limitation outcome (GROUND-R6). | FIX: Map `abstain` to RunStatus.DEGRADED with a coverage_caveat/limitation deliverable. Reserve AWAITING_APPROVAL strictly for an approval-gated plan deliverable that is policy-marked for approval (CKPT-R5), not for grounder abstention. | BYTES:     if decision is GroundDecision.ABSTAIN or _approval_required(state):
        status = RunStatus.AWAITING_APPROVAL
- [high] CKPT-R5 src/wattwise_core/agent/graph.py:326 â interrupt_gate is implemented only as a flag-stamping node plus `interrupt_before=["finalize"]`; there is no durable interrupt carrying a unique `interrupt_id`, no persisted proposed grounded plan, and no approve/reject/edit resume path anywhere in the agent module (grep for interrupt_id/Command/approve/principal returns nothing in agent code). CKPT-R5 requires the gate to pause with a durable interrupt that yields awaiting_approval carrying the grounded plan + thread_id + a unique interrupt_id, and to resume on a matching thread_id+interrupt_id decision without recomputing prior nodes. None of this exists, and the approval flag is driven by ABSTAIN, not by a plan policy-marked as requiring approval. | FIX: Implement a real HITL interrupt at interrupt_gate (langgraph `interrupt(...)`/durable interrupt) that mints a unique interrupt_id, persists the grounded plan in the agent-state store, and yields awaiting_approval at THIS node; add a principal-scoped resume entrypoint (CKPT-R9) accepting a matching thread_id+interrupt_id approve/reject/edit decision. Gate approval on the plan deliverable's approval policy, not on GroundDecision.ABSTAIN. | BYTES: def _interrupt_gate(state: AgentState) -> dict[str, Any]:
- [high] GRAPH-R5 src/wattwise_core/agent/graph.py:497 â GRAPH-R5/OUTCOME-R1 require that `awaiting_approval` is emitted by `interrupt_gate` (which PAUSES and does NOT reach `finalize`), while `finalize` emits only completed/degraded/budget_exceeded. Here the AWAITING_APPROVAL status is computed inside _finalize (line 371), and the graph pauses BEFORE finalize, so during the pause no status/outcome is produced at all; and after a human approves, finalize still recomputes AWAITING_APPROVAL (the ABSTAIN/approval flag persists), so an approved run cannot emit `completed`. This inverts the spec's node-to-status ownership. | FIX: Emit the awaiting_approval outcome at interrupt_gate when it pauses (carrying interrupt_id + grounded plan) and ensure finalize emits only completed/degraded/budget_exceeded. On resume after approval, finalize must transition to `completed` (or, on reject, finalize without persisting the plan). | BYTES:     interrupt_before = ["finalize"] if interrupt_on_approval else None
- [high] STATE-R4 src/wattwise_core/agent/contracts.py:67 â The immutable input fields `athlete_id` (line 67) and `idempotency_key` (line 72) are declared as bare TypedDict fields with NO reducer (`Annotated[str, write_once_reducer]`). STATE-R4 requires these be write-once and that 'Any node attempting to overwrite them MUST be rejected at the reducer level (the reducer MUST raise on a changed value)'. With LangGraph's default last-write-wins channel, a node-returned `athlete_id` would silently overwrite the authenticated identity â exactly the model/tool-set-identity bypass STATE-R4/AGT-SEC-R1 forbid. (The monotonic reducer exists only for the counters.) | FIX: Annotate athlete_id and idempotency_key with a write-once reducer that raises on any changed non-None value (mirroring the `_monotonic` pattern), so an attempted overwrite is rejected at the reducer, not merely by convention. | BYTES:     athlete_id: str
- [med] OUTCOME-R2 src/wattwise_core/agent/deliverables.py:292 â `thread_id` is read from `idempotency_key`, but `_build_inputs` (line 204) never sets `idempotency_key` (it sets only athlete_id/trigger/request_text/locale). So every AgentAnswer/Digest carries `thread_id=""`. OUTCOME-R2 requires every outcome carry the `thread_id`; CKPT-R2/-R3 require a stable thread_id scoped to (athlete_id, conversation_id). An empty thread id defeats resume, follow-ups (COACH-R8), and outcome addressing. | FIX: Thread the real langgraph thread_id (the (athlete_id, conversation_id)-derived id used by the checkpointer) through the run and read it onto the outcome; set idempotency_key/conversation_id in _build_inputs rather than aliasing a missing field. | BYTES: thread_id = _opt_str(final.get("idempotency_key")) or ""
- [med] COST-R4 src/wattwise_core/agent/graph.py:353 â The OSS graph ships no `budget_exceeded` path and no cost-admission/settle gate-sites at ingest_request/finalize. AGT-SEC-R3 / COST-R1/R2/R3/R4 require the `budget_exceeded` outcome variant and the node-boundary stop be present in OSS (so the commercial metered resolver plugs in WITHOUT an agent-graph change). `_finalize` can only emit COMPLETED/DEGRADED/AWAITING_APPROVAL; there is no node-boundary check that finalizes `budget_exceeded`, and no admission/settle gate call-site at node 1 / node 9. | FIX: Add OSS gate-site hooks (non-monetary no-op default) at ingest_request (admission) and finalize (settle), and a node-boundary guard that finalizes RunStatus.BUDGET_EXCEEDED carrying the best grounded partial when a plugged-in resolver signals a breach. | BYTES: def _finalize(state: AgentState) -> dict[str, Any]:
- [med] GROUND-R9 src/wattwise_core/agent/grounding.py:237 â A single CONTRADICTED number forces REPLAN unconditionally even though `_verify_number` (line 160-163) already computed the canonical replacement text and GROUND-R3 says 'a metric value not matching a computed analytic ... [is] replaced by the canonical value'. The aggregate discards that in-place correction and re-fetches instead of regenerating with the corrected value; combined with the finalize logic it never PROCEEDs. REFLECT-R2a maps `replan` to a non-terminal reflect cycle, so a draft whose only defect is a correctable number is pushed into the coverage-replan budget rather than a `regenerate` re-draft (redraft_count). The mapping of contradictedâreplan (vs regenerate) is not what GROUND-R3's replace-in-place intends. | FIX: Treat a contradicted-but-replaceable number as a `regenerate` (re-draft with the canonical value substituted, bounded by redraft_count); reserve `replan` for contradictions that require fetching different evidence. Never publish the contradicted value (already enforced) but use the canonical replacement. | BYTES: if has_contradicted:
        return GroundDecision.REPLAN
- [med] CKPT-R5 src/wattwise_core/agent/graph.py:444 â Plan approval is a build-time boolean, not policy-driven per deliverable, and the interrupt carries no `interrupt_id`, no persisted grounded plan payload, and no resume-with-decision path. CKPT-R5 requires the interrupt pause to persist the proposed grounded plan and yield an `awaiting_approval` outcome carrying the grounded plan + thread_id + a unique `interrupt_id`, resumable on a matching approve/reject/edit decision. CKPT-R9's principal-scoped resume (reject stale/mismatched interrupt_id) and CKPT-R6's grounded-only payload have no implementation in this epic. | FIX: Make interrupt_gate raise a langgraph durable interrupt carrying a unique interrupt_id + the grounded plan; persist it; implement principal-scoped resume (CKPT-R9) that validates thread_id+interrupt_id and rejects stale/consumed/mismatched ones. | BYTES: interrupt_on_approval: bool = False,
- [med] CKPT-R4 src/wattwise_core/agent/checkpoint.py:268 â No idempotency-key dedup is implemented. CKPT-R4 requires runs be idempotent per `idempotency key`: submitting the same request turn twice MUST resume/return the existing run rather than starting a duplicate within a configurable dedup window. The checkpointer keys only by thread_id; `idempotency_key` is a declared state field (contracts.py line 72) but is never persisted, indexed, or consulted to dedup a duplicate submission. | FIX: Persist the idempotency_key on the thread/run and, on submit, look up an in-window existing run for (athlete_id, idempotency_key) and resume/return it instead of starting a new run. | BYTES: async def aput(
- [med] STATE-R6 src/wattwise_core/agent/contracts.py:75 â The `retrieved` keyed-merge reducer (_keyed_merge, line 41-43) enforces NO maximum record count and NO maximum serialized size, and never drops lowest-relevance records or records truncation in coverage_gaps. STATE-R6 mandates the retrieved reducer 'MUST enforce a maximum record count and a maximum serialized size; on overflow it MUST drop lowest-relevance records ... and record the truncation in `coverage_gaps`.' State is therefore unbounded, also breaching the checkpointer size-budget intent. | FIX: Bound the keyed-merge reducer with configured max-count/max-bytes, dropping lowest-relevance (gather-assigned) records on overflow and appending a truncation marker into coverage_gaps. | BYTES: retrieved: Annotated[dict[str, Any], _keyed_merge]
- [med] MODEL-R3 src/wattwise_core/agent/model.py:93 â No real tokenizer-based context-budget enforcement exists on any model call. MODEL-R3 requires inputs be 'measured with the model's actual tokenizer (not a character heuristic) and assembled within the model's context window with headroom reserved for the structured output', with overflow handled by deterministic relevance-ranked trimming recorded in coverage_gaps. `compose`/`structured` send the assembled context verbatim with only an output max_tokens clamp; there is no input measurement, no window assembly, and no relevance-ranked trimming. | FIX: Measure assembled input with the target model's tokenizer, reserve output headroom, and trim lowest-relevance retrieved content (recording the trim in coverage_gaps) before the call; never pass unbounded context. | BYTES: async def compose(self, *, system: str, context: str, max_tokens: int = 1024) -> str:
- [med] REFLECT-R2 src/wattwise_core/agent/graph.py:249 â `reflect` does not produce the structured decision REFLECT-R2 mandates over the closed enum `replan | answer_with_caveat | give_up_gracefully`, and does not consider `coverage_gaps` to identify which capability requests to add/widen. It only increments reflection_count and always routes to plan_retrieval (_route_after_reflect, line 403 returns 'plan_retrieval' unconditionally). There is no `answer_with_caveat`/`give_up_gracefully` branch, so the reflect gate's closed-enum verdict (STRUCT-R1) is absent and the coverage cycle cannot terminate via answer_with_caveat as REFLECT-R2a requires. | FIX: Have reflect emit a provider-enforced structured decision over {replan, answer_with_caveat, give_up_gracefully} (considering coverage_gaps), and route replanâplan_retrieval, the other twoâcompose per the REFLECT-R2a cross-map. | BYTES: def _reflect(state: AgentState) -> dict[str, Any]:
- [med] OUTCOME-R4 src/wattwise_core/agent/graph.py:377 â The coverage_caveat is an untyped ad-hoc dict of raw gap strings, not the typed OUTCOME-R4 structure that names â in source-agnostic terms â which canonical inputs were missing/substituted/stale and at what fidelity. OUTCOME-R4 requires 'a structured value (not prose)'. `_open_gaps` returns whatever opaque strings assess_coverage emitted, so the caveat leaks coverage internals and is not the defined typed shape. | FIX: Emit a typed coverage_caveat model with source-agnostic missing/substituted/stale fields + fidelity, populated from canonical-input availability rather than raw gap tokens. | BYTES: caveat = {"open_gaps": _open_gaps(state)} if gaps_open else None
- [med] AGT-SEC-R2 src/wattwise_core/agent/graph.py:310 â `ground` only fills `grounded_text`; `grounded_html` (declared output, contracts.py line 81) is never set by any node, yet deliverables._outputs (line 286) reads `grounded_html` and falls back to text as HTML, and AgentAnswer exposes `answer_html`. AGT-SEC-R2 requires any HTML the model generates be sanitized server-side before it reaches a client; here model-composed prose is passed straight into an HTML field with no server-side sanitization in this epic, relying on an unverified 'sanitized later by the API' comment. No sanitize call exists in the agent path. | FIX: Either never populate an HTML body in the engine (prefer structured fields/text), or run a server-side HTML sanitizer before the body is exposed; do not alias raw grounded_text into answer_html unsanitized. | BYTES: "grounded_text": result.scrubbed_text,
- [med] STATE-R6 src/wattwise_core/agent/contracts.py:41 â The retrieved reducer enforces neither a maximum record count nor a maximum serialized size, and there is no overflow handling that drops lowest-relevance records and records the truncation in coverage_gaps. STATE-R6 mandates state be bounded with exactly that overflow behavior; here retrieved grows unbounded across re-gather cycles. | FIX: Add a max-record-count and max-serialized-size bound to _keyed_merge; on overflow drop lowest-relevance records (relevance assigned by gather) and append a truncation marker to coverage_gaps. | BYTES: def _keyed_merge(left: dict[str, Any], right: dict[str, Any]) -> dict[str, Any]:
    """retrieved reducer: merge by canonical record id; later replaces earlier 
- [med] MODEL-R3 src/wattwise_core/agent/graph.py:280 â No real tokenizer-based context budget exists. _render_context joins all retrieved records into one string with no input-token measurement (not even a char heuristic), no assembly within the model's context window, no headroom reserved for the structured output, and no deterministic relevance-ranked trimming recorded in coverage_gaps. MODEL-R3 requires inputs be measured with the model's ACTUAL tokenizer and trimmed lowest-relevance-first. | FIX: Measure assembled input with the target model's real tokenizer, reserve output headroom, and trim retrieved content lowest-relevance-first when over budget, recording each trim in coverage_gaps (never silent mid-record truncation). | BYTES: def _render_context(athlete_id: str, request_text: str | None, retrieved: Mapping[str, Any]) -> str:
    lines = [f"request: {request_text or ''}"]
    for key 
- [med] EVAL-R7 src/wattwise_core/eval/runner.py:48 â The eval datasets dir contains only grounding.json, abstention.json, injection.json. Required CI-gated suites are missing: EVAL-R7 termination (both reflection_count AND redraft_count bounds), EVAL-R2a memory non-substitution, EVAL-R3 intent/retrieval-plan accuracy (>=0.9), EVAL-R7a multilingual EN/DE/RU rendering, and EVAL-R5/-R5b qualitative LLM-judge + voice-liveness/length/follow-up. The runner's _SUITE_GRADERS only knows grounding/abstention/injection; no termination/intent/multilingual/judge graders or datasets exist. | FIX: Add the missing versioned datasets and graders: a termination suite exercising both bounded counters, a memory non-substitution case, an intent/plan-accuracy suite with the >=0.9 gate, a multilingual rendering suite, and the qualitative judge (structured-output, never certifying grounding/abstention/injection/status). | BYTES: _DATASETS_DIR = Path(__file__).parent / "datasets"
- [med] GROUND-R8 src/wattwise_core/eval/runner.py:262 â The eval suite re-implements its OWN grounder (_classify_claim) instead of exercising the production ground() in agent/grounding.py. GROUND-R8 requires grounding be covered by golden-reference + property tests (planted hallucinations scrubbed, known-good unchanged). Gating CI on a parallel reference grounder means the actual production grounder is not what the suite proves; a divergence between the two would pass CI while shipping a hallucination. | FIX: Drive the EVAL-R4/-R8 grounding suite through the production wattwise_core.agent.grounding.ground() (and CanonicalEvidence) so the gated code is the shipped grounder, not a reimplementation. | BYTES: def _classify_claim(
    claim: Claim,
    metrics: dict[str, float],
    allowed_urls: set[str],
    tolerance: float,
) -> _GroundedClaim:
- [med] OUTCOME-R2 src/wattwise_core/agent/deliverables.py:292 â thread_id is derived from the idempotency_key field. OUTCOME-R2 requires the outcome carry the thread_id, and CKPT-R2/-R3 define thread_id as the stable identifier scoped to (athlete_id, conversation_id) â a distinct concept from the per-turn idempotency key (CKPT-R4). Using idempotency_key as thread_id conflates two identifiers; a follow-up resuming the same durable thread (COACH-R8) cannot be addressed correctly, and awaiting_approval/resume (CKPT-R5/-R9) would mis-key the thread. | FIX: Carry the real thread_id through state/outcome (the (athlete_id, conversation_id)-scoped id used by the checkpointer) and read it here, not the idempotency_key. | BYTES:     thread_id = _opt_str(final.get("idempotency_key")) or ""
- [med] OUTCOME-R2 src/wattwise_core/agent/graph.py:353 â The run outcome assembled at finalize carries neither thread_id nor (for awaiting_approval) interrupt_id, and no per-run cost/latency rollup. OUTCOME-R2 requires every outcome carry at minimum status, thread_id, grounded deliverable/partial, the per-run cost/latency rollup, and citations; the awaiting_approval variant MUST additionally carry interrupt_id and the grounded proposed plan. interrupt_id is never minted anywhere in the engine. | FIX: Emit thread_id and a cost/latency rollup on every outcome at finalize, and emit interrupt_id + the grounded proposed plan on the awaiting_approval variant at interrupt_gate. | BYTES: def _finalize(state: AgentState) -> dict[str, Any]:
    ... return {"status": status, "coverage_caveat": caveat}
- [med] GROUND-R9 src/wattwise_core/agent/graph.py:406 â When the grounder returns `replan` (or `regenerate`) but the bound is already exhausted, `_route_after_ground` falls through to `interrupt_gate` and `_finalize` then derives the terminal status from the last ground decision. A `replan`/`regenerate` decision that is bound-exhausted is NOT one of `abstain`/`proceed`, so in `_finalize` it takes neither the ABSTAIN branch nor PROCEED; with gaps closed it can fall through to COMPLETED even though the draft still carried ungrounded/contradicted claims that were never re-grounded. GROUND-R9/REFLECT-R4 require a draft that exhausts redraft_count to fall through to `replan` (if reflection_count remains) or `abstain` (degraded/limitation), never to a complete-looking answer. | FIX: In _finalize, treat a non-PROCEED last-ground decision (regenerate/replan) at bound exhaustion as DEGRADED (or route to abstain), so an un-grounded bound-exhausted draft can never finalize COMPLETED. | BYTES: def _route_after_ground(state: AgentState) -> str:
- [med] STATE-R3 src/wattwise_core/agent/graph.py:235 â `assess_coverage` writes freshly computed gaps into `coverage_gaps`, but the STATE-R3 reducer for that field is set-union (`_set_union`, contracts.py:46-48), so gaps accumulate and are NEVER cleared once added. After a successful re-gather closes a gap, the cumulative `coverage_gaps` field still reports it open. The node works around its own reducer by routing off a separate `coverage` message marker (`_open_gaps`) instead of the state field, and `_finalize` then builds the OUTCOME-R4 coverage_caveat from the (correct) marker â but the typed `coverage_gaps` state field itself is monotonically stale, contradicting STATE-R3's intent that the field reflect current coverage and undermining any consumer that reads it. | FIX: Either make the routing/caveat read the cumulative field consistently, or change coverage_gaps semantics so closed gaps are removable (e.g. last-write-wins for the current open set), so the typed state field and the message marker cannot diverge. | BYTES: return {"coverage_gaps": set(gaps), "messages": [marker]}
- [med] INJECT-R1 src/wattwise_core/agent/graph.py:280 â The compose context concatenates the untrusted user `request_text` and raw `retrieved` record bodies (which per INJECT-R1 include source-derived free text / tool-result bodies) into one flat string with no delimited data envelope. INJECT-R1 requires untrusted content be wrapped in clearly delimited data envelopes and never merged with instructions; INJECT-R5 requires tool-result bodies be wrapped as untrusted data. While model.compose places `context` in a separate user message (a partial mitigation), there is no envelope marking the request/record bodies as data-to-analyze-not-instructions, so the structural injection-isolation the spec mandates is not implemented at this seam. | FIX: Wrap request_text and each retrieved record body in explicit delimited untrusted-data envelopes (e.g. <untrusted-data>...</untrusted-data> fences) before passing to compose, mirroring the ToolResult.untrusted marker, so injected instructions in titles/notes/tool bodies cannot read as instructions. | BYTES: def _render_context(athlete_id: str, request_text: str | None, retrieved: Mapping[str, Any]) -> str:
    """Serialise canonical evidence for the composer (pure,
- [med] OUTCOME-R2 src/wattwise_core/agent/deliverables.py:292 â `_outputs` derives the deliverable `thread_id` from `final.get("idempotency_key")`, but `_build_inputs` (lines 204-210) constructs the run state with only `athlete_id`, `trigger`, `request_text`, `locale` â it never sets `idempotency_key`. `AgentState` also has no `thread_id` field (the langgraph thread id lives in the run config, not state). So `final.get("idempotency_key")` is always None and `thread_id` is always the empty string for both `answer_question` and `weekly_digest`. OUTCOME-R2 requires every outcome carry the `thread_id`; an empty thread_id also breaks COACH-R8 follow-ups and CKPT-R2 resume (a follow-up cannot target the durable thread). | FIX: Thread the real durable `thread_id` (the langgraph config thread / `(athlete_id, conversation_id)` per CKPT-R3) through the deliverable seam and project it onto the outcome, rather than reading a never-populated `idempotency_key`. If idempotency_key is intended as the thread key, set it in `_build_inputs` and ensure it equals the checkpointer thread id. | BYTES:     thread_id = _opt_str(final.get("idempotency_key")) or ""
- [med] EVAL-R4 src/wattwise_core/eval/grading.py:31 â The grounding gate accepts faithfulness >= 0.99. EVAL-R4 (owned by this doc) states: "Grounding stage MUST scrub 100% of planted hallucinations + leave 100% of planted-valid claims intact. Any leaked hallucination -> fail CI", and AGT-AC-5 repeats "100% planted hallucinations scrubbed + 100% planted-valid survive". A 0.99 faithfulness band admits a 1% case-level miss of leaving-valid-intact / scrubbing without tripping the gate (the `fabricated == 0` guard only catches surfaced non-canonical NUMBERS, not a failed-to-scrub name/URL or a dropped valid claim, which land in `failures`/`grounded_correct` but can still pass at >= 0.99). The threshold should be 1.0 per the doc-50 EVAL-R4 mandate. | FIX: Set GROUNDING_MIN_FAITHFULNESS = 1.0 so any planted-hallucination leak OR any planted-valid drop fails CI, matching EVAL-R4 / AGT-AC-5's literal 100%. If doc 80 QA-EVAL-R6 truly relaxes to 99%, reconcile the two docs; as written the doc-50-owned EVAL-R4 is the binding 100% mandate. | BYTES: GROUNDING_MIN_FAITHFULNESS = 0.99
- [med] VOICE-R7 /home/bepcyc/dev/my/prod/wattwise/wattwise-core/src/wattwise_core/agent/deliverables.py:204 â The VOICE-R7 number-density CAP (<=2 short / <=3 standard / <=4 detailed) is never enforced on the shipping deliverable. _build_inputs assembles graph state with no response_length/number-cap field (AgentState in contracts.py:58-87 has none), and answer_question accepts response_length only to discard it here, so compose (graph.py:265-275, calling model.compose with no length/cap) cannot foreground-limit numbers. The deterministic gate functions count_foregrounded_numbers, number_cap, and leads_with_state are pure helpers exercised only by tests on hand-built strings; they are never invoked inside answer_question/weekly_digest to assert/limit the projected body. EVAL-R5b.1 names these as the gate for leads-with-state and number-count-under-ceiling, but no production path applies them, so the cap is unenforced. | FIX: Thread response_length into the graph state (add a typed field to AgentState; pass it from _build_inputs/answer_question), have compose honor the per-length ceiling, and apply count_foregrounded_numbers(...) <= number_cap(length) and leads_with_state(...) in the deliverable projection (or a ground-stage gate) so a violating draft is corrected, not only test-asserted. | BYTES: state: AgentState = {
        "athlete_id": athlete_id,
        "trigger": trigger,
        "request_text": request_text,
        "locale": locale,
    }
    re
- [med] EVAL-R5 src/wattwise_core/eval/__init__.py:18 â The LLM-as-judge qualitative rubric suite (EVAL-R5: coherence/tone/coach-voice/actionability/pedagogical clarity, judge using provider-enforced structured output, scores-below-threshold-fail-CI) and the EVAL-R5b voice-liveness/length-monotonicity/follow-up evals are absent. No judge grader, no rubric, no readiness-state-first deterministic check exists in src/wattwise_core/eval. The doc-self-description even enumerates only three datasets, confirming the qualitative suites were never built. | FIX: Add the EVAL-R5 judge rubric (structured-output scored, external rubric config) plus the EVAL-R5b deterministic leads-with-state / number-density checks, catalogued in QA-EVAL-R2 and gated in QA-EVAL-R6. | BYTES: * ``datasets/`` â versioned checked-in cases: grounding/faithfulness, abstention /
  fail-closed (QA-EVAL-R2.2), and a prompt-injection corpus (INJ-R2 / INJECT-
- [med] EVAL-R7a src/wattwise_core/eval/datasets/grounding.json:12 â The multilingual-rendering suite (EVAL-R7a: same grounded fixtures rendered in EN/DE/RU carry the SAME numeric values/citations, stay jargon-free, no untranslated internal string, and a requested-but-unloaded language exercises the LANG-R4 fallback) is missing. Every case in all three datasets is `locale: en` (no `de`/`ru` fixtures, no fallback case), and the runner ignores `locale` entirely. The EN/DE/RU language-parity and fallback guarantees are not CI-gated. | FIX: Add EN/DE/RU rendering fixtures (plus an unloaded-language fallback case) asserting identical grounded numbers/citations across languages and no untranslated internals, and assert it deterministically. | BYTES: "locale": "en",
- [med] GROUND-R7 src/wattwise_core/eval/datasets/abstention.json:16 â The abstention dataset declares an `unavailable` map (the GROUND-R7 'metric whose canonical computation is unavailable MUST state unavailable, never a placeholder/zero' signal), but the runner never reads it (`rg unavailable` in runner.py = none). Abstention is decided purely by `abstained = not survivors` (runner.py L248) â i.e. by whether any candidate claim happened to ground. Cases with empty `candidate_claims` (abstain-hrv-missing, abstain-cp-poor-fit, abstain-out-of-scope-medical) pass trivially regardless of whether the unavailable-metric path is correctly handled, so the GROUND-R7 'unavailable -> state unavailable' obligation is not actually exercised. | FIX: Have the runner consume `unavailable` and assert the pipeline surfaces an explicit unavailability statement (not a placeholder/zero) for those metrics, not merely that no number was published. | BYTES: "unavailable": {"hrv_rmssd_ms": "missing_required_input"},
- [med] STATE-R3 src/wattwise_core/agent/contracts.py:73 â STATE-R3 enumerates `cost_events` as a required accumulating field with an append-only reducer; AGT-OBS-R2 records per-call cost/token events. The AgentState schema declares messages/retrieved/coverage_gaps/reflection_count/redraft_count but NO `cost_events` field (grep confirms cost_events absent from contracts.py). The cost/latency rollup and AGT-OBS-R5 cost events have no typed state channel. | FIX: Add `cost_events: Annotated[list[...], _append]` (append-only reducer per STATE-R3) to AgentState so per-call cost/token events accumulate in typed state and feed the OUTCOME-R2 rollup and AGT-OBS spans. | BYTES:     reflection_count: Annotated[int, _monotonic]
    redraft_count: Annotated[int, _monotonic]
- [med] COST-R2 src/wattwise_core/agent/graph.py:156 â GRAPH-R2 node1 (ingest_request) and node9 (finalize) MUST host the cost-admission and cost-settle GATE POINTS in OSS (COST-R1/R2/R3, AGT-SEC-R3), even as non-monetary no-ops enforcing rate + local guards (max steps / wall-clock / tokens / tool-iterations). _ingest_request only records the inbound message and _finalize only derives status; neither routes through any admission/settle resolver, and no local guard (max-steps/wall-clock/tokens/tool-iterations) exists. The `budget_exceeded` RunStatus is declared but unreachable â no node can emit it â so the commercial metered resolver cannot plug in 'WITHOUT an agent-graph change' (AGT-ENT-R4/COST-R1). | FIX: Add a cost-admission gate call-site in ingest_request and a settle gate in finalize (OSS default = non-monetary no-op via an injected resolver seam), wire the optional local guard, and make a per-run guard breach finalize as budget_exceeded at a node boundary (COST-R4), so the seam exists in OSS without a graph change for the commercial resolver. | BYTES: def _ingest_request(state: AgentState) -> dict[str, Any]:
- [med] EVAL-R7 src/wattwise_core/eval/runner.py:382 â The offline suite ships only grounding/abstention/injection datasets and graders. EVAL-R7 (termination test of BOTH reflection_count and redraft_count bounds), EVAL-R2a (memory non-substitution: memory holds a stale canonical value, answer must cite the LIVE canonical value), and EVAL-R3 (intent/retrieval-plan accuracy >= 0.9) are required CI-gated eval cases with no dataset/grader present here. The reference pipeline also does not exercise the bounded recovery counters or the planner. | FIX: Add EVAL-R7 termination fixtures (perpetually-insufficient coverage -> degraded at reflection_count; perpetually-failing-to-ground -> redraft_count bound -> replan/abstain, never budget_exceeded), an EVAL-R2a memory-non-substitution case, and an EVAL-R3 intent/plan-accuracy dataset+grader, all wired into the CI gate. | BYTES: _SUITE_GRADERS: dict[str, frozenset[str]] = {
    "grounding": ...,
    "abstention": ...,
    "injection": ...,
}
- [med] GROUND-R9 src/wattwise_core/agent/grounding.py:237 â Any single `contradicted` claim forces the aggregate decision to REPLAN unconditionally, even though _verify_number already scrubbed/replaced the contradicted number with the canonical value (so the offending claim is no longer published). GROUND-R9 weights `contradicted` with the strongest penalty but the aggregate decision is bounded proceed/regenerate/replan/abstain driven by the whole draft; forcing REPLAN whenever a number was merely corrected-in-place can spin the ground->reflect->plan_retrieval cycle on a fully-correctable draft (contributing to reflection_count exhaustion) rather than regenerating/proceeding with the corrected text. | FIX: Distinguish 'contradicted-and-corrected-in-place' (number replaced by canonical -> regenerate/proceed with the corrected draft) from 'contradicted-and-unrecoverable' (-> replan for missing/correct evidence). Don't force a replan cycle when the contradiction was already resolved by canonical substitution; keep the strongest penalty only against publishing the contradicted value (already enforced). | BYTES:     if has_contradicted:
        return GroundDecision.REPLAN
## API: 67 findings (25 high, 35 med, 7 low)
- [high] ERR-R9 src/wattwise_core/api/routers/performance.py:161 â The fail-closed analytics path raises a raw fastapi HTTPException with a dict detail carrying type=analytics-precondition-unmet and the machine errors[].code (cp_insufficient_points/hrv_dsp_unavailable/pmc_seed_unavailable/trimp_*). But the registered StarletteHTTPException handler (_on_http_exception, errors.py:255-258) maps PURELY by status code (422 -> 'validation-error') and assembles with detail=None, discarding the dict. So the client receives type=validation-error with NO errors[].code at all. ERR-R9 (and appendix sec3/sec8/sec10, API-R30) REQUIRE 422 analytics-precondition-unmet with the machine-readable errors[].code. The intended type/code never reaches the wire. | FIX: Raise ProblemError('analytics-precondition-unmet', errors=[FieldError(code=code, message=detail)]) instead of HTTPException, so the catalog type and errors[].code survive serialization. | BYTES: return HTTPException(
        status_code=HTTPStatus.UNPROCESSABLE_ENTITY,
        detail={
            "type": "analytics-precondition-unmet",
            "err
- [high] PAGE-R5 src/wattwise_core/api/routers/activities.py:232 â A tampered/expired cursor raises HTTPException(400, detail='invalid-cursor'), but _on_http_exception maps status 400 -> 'validation-error' (errors.py _STATUS_TO_SLUG: BAD_REQUEST: 'validation-error') and drops the detail. So the emitted problem type is 'validation-error', NOT the PAGE-R5-required 'invalid-cursor'. Clients branching on type (ERR-R3) cannot detect a bad cursor. | FIX: Raise ProblemError('invalid-cursor') from the cursor decode failure so the correct closed problem type is emitted. | BYTES: raise HTTPException(status_code=HTTPStatus.BAD_REQUEST, detail="invalid-cursor") from exc
- [high] PAGE-R5 src/wattwise_core/api/routers/activities.py:222 â The cursor is plain unsigned base64-encoded JSON. PAGE-R5 requires an 'opaque, SIGNED, server-issued token'. With no HMAC/signature, a client can hand-craft any {t,id} cursor (it is not opaque and not tamper-evident), and _decode_cursor (line 227) accepts any well-formed base64 JSON without integrity check. PAGE-R5's 'a tampered cursor SHALL yield 400 invalid-cursor' cannot be enforced because tampering is undetectable; clients can also construct cursors (forbidden by PAGE-R5). | FIX: Sign the cursor payload (HMAC over the JSON with the engine signing key) and verify the signature in _decode_cursor; reject a bad signature with ProblemError('invalid-cursor'). | BYTES: def _encode_cursor(start_time: _dt.datetime, activity_id: str) -> str:
    raw = json.dumps({"t": start_time.isoformat(), "id": activity_id}).encode()
    retur
- [high] API-R48 src/wattwise_core/api/routers/activities.py:403 â base_values is emitted as the decimated sample INDICES, not the documented X-axis values. API-R48 requires base_values[] to be 'seconds from activity.start_time' for base=time or 'cumulative metres (distance_m)' for base=distance. For base=distance the indices are flatly wrong (index != metres) and the distance_m channel is ignored; for base=time at 1 Hz they only coincidentally resemble seconds and are not derived from start_time. The validated 'base' param (line 349) is never used to compute base_values. | FIX: Compute base_values from the actual time channel (seconds from start_time) or the cumulative distance_m channel per the selected base, then decimate that array with the shared index set. | BYTES: activity_id=str(act.activity_id), base=base, base_values=[float(i) for i in idx],
- [high] DOC-R3 src/wattwise_core/api/routers/performance.py:209 â None of the 9 performance routes (load-fitness/load-metrics/critical-power/power-curve/coggan/w-balance/hrv/aerobic-decoupling/trimp) nor the 5 activities routes pass operation_id. Generating the OpenAPI from these routers yields FastAPI auto-ids e.g. operationId='load_fitness_v1_performance_load_fitness_get' (verified by building app.openapi()). DOC-R3 mandates every operation declare a STABLE operationId used to name generated client methods; an auto-mangled, path-embedded id is not stable and breaks DOC-R5 'no manual fixups' typed-client generation. | FIX: Add operation_id="getLoadFitness" (and stable ids for all 14 performance+activities ops) to each @router.get decorator, matching the connections/imports/sync/onboarding routers that already do. | BYTES: @router.get("/load-fitness", response_model=ChartSeries, dependencies=[_Read])
- [high] DOC-R4 src/wattwise_core/api/routers/performance.py:209 â The emitted OpenAPI has NO reusable 'Problem' component and NO 'PageEnvelope' component (verified: spec['components']['schemas'] lacks both; the only documented error response is 422 -> $ref '#/components/schemas/HTTPValidationError' with content-type application/json, not application/problem+json). DOC-R4 requires a reusable Problem component (RFC9457), a PageEnvelope component, and that ALL error responses $ref Problem and ALL collections compose PageEnvelope. None of the closed-catalog error types (401/403/404/409/429/413/415/500/502) are documented on any operation. | FIX: Register a Problem component + PageEnvelope component and declare per-route `responses={401:..,403:..,404:..,422:..}` all $ref-ing Problem; wrap list responses in a PageEnvelope component instead of ad-hoc ActivityList.Page. | BYTES: response_model=ChartSeries, dependencies=[_Read]
- [high] SCHEMA-R1 src/wattwise_core/api/routers/agent_routes.py:437 â agent_ask declares no response_model and is typed `-> Any`; the generated OpenAPI 200 schema is the untyped object {"title": "Response Agentask"} (verified). SCHEMA-R1 forbids any endpoint returning an untyped/free-form object and requires a NAMED 2xx json schema; DOC-R5 then fails (TS generation produces `any`, an unguarded fixup). The AgentAskResponse model exists but is never bound as response_model. | FIX: Set response_model=AgentAskResponse on @router.post('/ask') (and handle the SSE branch via a separate documented text/event-stream response) so the 200 references the named schema. | BYTES: @router.post("/ask", dependencies=[_Agent], operation_id="agentAsk")
- [high] ERR-R9 src/wattwise_core/api/routers/performance.py:161 â Analytics preconditions (hrv_dsp_unavailable, cp_insufficient_points, pmc_seed_unavailable, trimp_*) raise a raw FastAPI HTTPException carrying a dict detail with type=analytics-precondition-unmet. But the installed handler errors._on_http_exception maps purely by status code (_STATUS_TO_SLUG[422]='validation-error') and discards the detail dict (detail=None). Verified end-to-end: the response body is type 'validation-error', NOT 'analytics-precondition-unmet', and the machine errors[].code (hrv_dsp_unavailable/cp_insufficient_points) is silently dropped. ERR-R9/ERR-R3 are unsatisfied â clients cannot branch on the required code. | FIX: Raise the catalog ProblemError('analytics-precondition-unmet', errors=[FieldError(code=...)]) instead of HTTPException(detail={...}); same for the activities/streams 422s. | BYTES: return HTTPException(
        status_code=HTTPStatus.UNPROCESSABLE_ENTITY,
        detail={
            "type": "analytics-precondition-unmet",
            "err
- [high] ERR-R5 src/wattwise_core/api/errors.py:220 â The redaction module (api/redaction.py redact_text/redact_payload) is dead code: rg confirms redact_text(/redact_payload( are called nowhere outside redaction.py. The error _assemble passes caller-supplied `detail` through verbatim with no redaction. Verified: ProblemError('internal-error', detail='failed for user a@b.com bearer sk-ABCDEFGHIJKLMNOP') renders that PII+token leak unmasked in the 500 body. Router call sites pass adapter/processor-supplied strings into detail (e.g. imports._rejected(detail=reason)), so a source-derived filename/token can leak. Violates ERR-R5 (no secrets/tokens leak) and API-R19 (PII redaction in problem docs). | FIX: Apply redact_text() to detail and redact each FieldError.message in _assemble/_render before serialization, wiring the existing redaction module into the error path. | BYTES: detail=detail if detail is not None else problem_type.title,
- [high] AUTH-R6 src/wattwise_core/api/app.py:135 â POST /v1/auth/token is public (AUTH-R10) but performs NO credential verification. issue_token() takes no request body and unconditionally mints a FULL-scope (read write agent sync export admin) owner bearer token to any caller that can reach the endpoint. API-R23 requires issuance only for 'a valid sign-in/exchange of the platform's first-party credential' with 'Invalid credentials -> 401 unauthenticated'. This is a fail-open identity bypass: the entire authenticated surface is reachable by anyone who hits the public token endpoint. | FIX: Require a first-party credential on POST /v1/auth/token (e.g. an owner secret / sign-in exchange validated against config), and return ProblemError('unauthenticated', headers=_BEARER_CHALLENGE) on failure with no unknown-user-vs-wrong-secret distinction (API-R23). Only mint the token after the credential verifies. | BYTES: tokens = issue_access_token(settings, subject="owner", scopes=_OWNER_SCOPES)
- [high] ERR-R9 src/wattwise_core/api/routers/performance.py:161 â The precondition-unmet error is raised as a raw HTTPException(422) carrying a detail dict. The registered handler errors._on_http_exception maps PURELY by exc.status_code via _STATUS_TO_SLUG, where 422 -> 'validation-error', and calls _assemble(detail=None, errors=()). The dict (type 'analytics-precondition-unmet' and the machine errors[].code such as cp_insufficient_points / hrv_dsp_unavailable / pmc_seed_unavailable / trimp_*) is silently discarded. Clients receive type=validation-error with no machine code, so the fail-closed analytics contract (ERR-R9/ERR-R8) is not met on the wire. | FIX: Raise ProblemError('analytics-precondition-unmet', errors=[FieldError(code=code, message=detail, parameter=...)]) instead of HTTPException, so the closed-catalog slug and errors[].code reach the client. Same pattern needed for date_range()'s validation HTTPException (line 176) and every _precondition_unmet call site. | BYTES: return HTTPException(
        status_code=HTTPStatus.UNPROCESSABLE_ENTITY,
        detail={
            "type": "analytics-precondition-unmet",
            "err
- [high] PAGE-R5 src/wattwise_core/api/routers/activities.py:222 â PAGE-R5 requires the cursor to be 'an opaque, SIGNED, server-issued token' that the client 'MUST NOT construct'; a tampered cursor SHALL yield 400 invalid-cursor. This cursor is plain unsigned base64-encoded JSON. _decode_cursor (line 227) accepts ANY well-formed base64 JSON {t,id}, so a client can forge arbitrary cursors (any start_time/id) and have them applied to the keyset query. There is no signature/HMAC and no tamper detection. | FIX: Sign the cursor payload server-side (e.g. HMAC over the JSON with token_signing_key) and verify the signature in _decode_cursor; reject an unsigned/tampered cursor with ProblemError('invalid-cursor') (400). Also bind it to the request's filter/sort params per PAGE-R6. | BYTES: def _encode_cursor(start_time: _dt.datetime, activity_id: str) -> str:
    raw = json.dumps({"t": start_time.isoformat(), "id": activity_id}).encode()
    retur
- [high] ERR-R9 src/wattwise_core/api/routers/performance.py:159 â _precondition_unmet raises a raw Starlette HTTPException whose `detail` dict carries type=analytics-precondition-unmet and the machine code (cp_insufficient_points/hrv_dsp_unavailable/pmc_seed_unavailable). But the only StarletteHTTPException handler (errors.py:246 _on_http_exception) maps purely by status_code via _STATUS_TO_SLUG[422]='validation-error' and discards exc.detail (calls _assemble(..., detail=None)). The client therefore receives type=validation-error with NO errors[] array and NO machine code. ERR-R9 (and API-R30 fail-closed) REQUIRE `422 analytics-precondition-unmet` with a machine-readable errors[].code; API-R20 item 5 tests it. The fail-closed analytics contract is silently downgraded to a generic validation error. | FIX: Raise ProblemError('analytics-precondition-unmet', errors=[FieldError(code=code, message=detail, parameter=...)]) instead of a raw HTTPException, so the catalog slug and errors[] survive the handler. | BYTES: return HTTPException(
        status_code=HTTPStatus.UNPROCESSABLE_ENTITY,
        detail={
            "type": "analytics-precondition-unmet",
            "err
- [high] PAGE-R5 src/wattwise_core/api/routers/activities.py:232 â A tampered/expired cursor raises HTTPException(400, detail='invalid-cursor'). The StarletteHTTPException handler maps by status code: _STATUS_TO_SLUG[400]='validation-error' (errors.py:335) and discards the 'invalid-cursor' detail string. The emitted problem `type` is therefore https://wattwise.app/problems/validation-error, NOT invalid-cursor. PAGE-R5 and ERR-R8 mandate `400` with problem type `invalid-cursor`; API-R20 item 3 explicitly tests tampered/expired cursor -> 400 invalid-cursor. Clients branching on `type` (ERR-R3) will never see invalid-cursor. | FIX: Raise ProblemError('invalid-cursor') (status 400 already in catalog) instead of the raw HTTPException. | BYTES: raise HTTPException(status_code=HTTPStatus.BAD_REQUEST, detail="invalid-cursor") from exc
- [high] ERR-R6 src/wattwise_core/api/routers/activities.py:443 â _validation (used for bad base/max_points/channels and from>to) embeds errors:[{parameter}] in the HTTPException detail dict, but _on_http_exception discards detail entirely (detail=None) and emits no errors[]. So a 422 for an unknown stream channel / bad base / bad max_points returns type=validation-error with an EMPTY body and no `parameter` locator. ERR-R6 requires the 422 validation-error to carry a populated errors[] with the offending `parameter`. Same defect in performance.py:170 date_range (from>to) which also embeds errors in HTTPException.detail and loses it. API-R20 item 9 (from>to -> 422, unknown enum value -> 422) and item 28 (bad base/max_points/channel -> 422) expect the field locator. | FIX: Raise ProblemError('validation-error', errors=[FieldError(code='out_of_range'|'invalid', message=..., parameter=parameter)]) at each raise site (activities._validation, activities._not_found path is fine, performance.date_range, performance._precondition_unmet) so errors[] survives. | BYTES: def _validation(parameter: str) -> HTTPException:
    return HTTPException(
        status_code=HTTPStatus.UNPROCESSABLE_ENTITY,
        detail={"type": "valida
- [high] API-R51 src/wattwise_core/api/routers/activities.py:200 â Unknown activity/file ids raise HTTPException(404, detail='not-found'). _STATUS_TO_SLUG[404]='not-found' so the slug happens to be right, but the handler still discards exc.detail and uses catalog copy â works only by status-code coincidence. More importantly this pattern (raw HTTPException for domain errors instead of ProblemError) is the same root defect as the cursor/precondition cases and is fragile: any status whose _STATUS_TO_SLUG mapping differs from the intended slug (400->validation-error, 422->validation-error) silently mis-types. The activities router uses raw HTTPException for ALL its errors (404/400/422) while every other router (connections/sync/imports/agent) correctly uses ProblemError; activities and performance are the two non-conformant routers. | FIX: Replace all raw HTTPException raises in activities.py and performance.py with ProblemError(<slug>, errors=...) so type and errors[] are catalog-driven, not derived from a lossy status->slug table. | BYTES: def _not_found() -> HTTPException:
    return HTTPException(status_code=HTTPStatus.NOT_FOUND, detail="not-found")
- [high] ERR-R9 src/wattwise_core/api/routers/performance.py:161 â Fail-closed analytics raises a framework HTTPException whose structured detail carries type=analytics-precondition-unmet + machine code. But the registered handler _on_http_exception (errors.py:255-258) maps purely on status code (_STATUS_TO_SLUG[422]='validation-error') and forces detail=None,errors=() â discarding the whole detail. The response type becomes validation-error, and the mandated machine errors[].code (cp_insufficient_points / hrv_dsp_unavailable / pmc_seed_unavailable / trimp_missing_thresholds / trimp_hr_domain_invalid) is lost entirely. ERR-R9 requires 422 analytics-precondition-unmet WITH a machine errors[].code. (The integration test passes only because its fixture builds a bare FastAPI() without install_error_handlers, so FastAPI's default handler echoes the dict â which is itself ERR-R1 non-conformant.) | FIX: Raise ProblemError('analytics-precondition-unmet', errors=[FieldError(code=code, message=detail)]) instead of HTTPException; ProblemError routes through _on_problem_error which preserves the slug, 422 status, and errors[]. Add 'analytics-precondition-unmet' default status (422) to the catalog (already present) so the type and code survive. | BYTES: return HTTPException(
        status_code=HTTPStatus.UNPROCESSABLE_ENTITY,
        detail={
            "type": "analytics-precondition-unmet",
            "err
- [high] ERR-R1 src/wattwise_core/api/routers/activities.py:443 â activities.py and performance.py raise framework HTTPException with a structured/string detail rather than the catalog ProblemError used everywhere else (sync/imports/connections). Under the production _on_http_exception handler the structured detail (type slug + errors[]) is discarded; under the bare-FastAPI path the integration tests use, the body is FastAPI's default {"detail": ...} with media type application/json â NOT RFC 9457 application/problem+json. Either way a non-2xx leaving these routers is not the single uniform problem+json document ERR-R1/ERR-R2 mandate. | FIX: Replace every HTTPException raise in activities.py/performance.py with ProblemError(slug, errors=[FieldError(...)]) (the path that emits application/problem+json with type/title/status/detail/instance/trace_id). Never raise framework HTTPException with structured detail in routers. | BYTES: def _validation(parameter: str) -> HTTPException:
    return HTTPException(
        status_code=HTTPStatus.UNPROCESSABLE_ENTITY,
        detail={"type": "valida
- [high] PAGE-R5 src/wattwise_core/api/routers/activities.py:232 â A tampered/expired cursor raises HTTPException(400, detail='invalid-cursor'). The production handler _on_http_exception maps 400 via _STATUS_TO_SLUG[HTTPStatus.BAD_REQUEST]='validation-error' (errors.py:335) and discards the 'invalid-cursor' detail string. The emitted problem type is therefore https://wattwise.app/problems/validation-error, NOT the mandated invalid-cursor. PAGE-R5 (and ERR-R8) require a tampered/expired cursor to yield 400 with problem type invalid-cursor â clients branching on type (ERR-R3) will misclassify it. | FIX: Raise ProblemError('invalid-cursor') (catalog status 400) instead of HTTPException, so the invalid-cursor slug is preserved. | BYTES: raise HTTPException(status_code=HTTPStatus.BAD_REQUEST, detail="invalid-cursor") from exc
- [high] API-R23 src/wattwise_core/api/app.py:133 â Fail-open identity issuance. `POST /v1/auth/token` accepts NO body/credential and unconditionally mints a full-scope owner token (read|write|agent|sync|export|admin) to any anonymous caller. API-R23 requires exchanging a valid first-party credential and returning `401 unauthenticated` on invalid credentials. As written, any unauthenticated request to the public token endpoint yields an all-capability token â a complete auth bypass for the whole `/v1` surface. | FIX: Require a first-party credential (a request body carrying the owner sign-in secret) and verify it against a configured secret (e.g. constant-time compare to a hashed `owner_secret` in Settings) BEFORE calling `issue_access_token`; on mismatch/absence raise `ProblemError('unauthenticated', headers=_BEARER_CHALLENGE)` with no unknown-user/wrong-secret distinction. | BYTES: async def issue_token(settings: AppSettings) -> dict[str, object]:
        """Issue a first-party access token for the single owner (API-R23/R24)."""
        to
- [high] LIMIT-R1 src/wattwise_core/api/routers/connections.py:251 â LIMIT-R1 ("All endpoints SHALL be rate-limited per athlete/owner") is unmet for every non-agent surface. The RateLimiter is only invoked in agent_routes (AGENT class, agent_routes.py:455). connections, imports, sync, onboarding, activities, performance, and `/v1/auth/token` apply no limiter â no app-level middleware exists in app.py and `LimitClass.READ`/`LimitClass.MUTATING` are never referenced outside ratelimit.py. These endpoints can be called without any per-athlete request-rate ceiling. | FIX: Apply rate limiting to all routes â either install an ASGI middleware that classifies the route (read vs mutating) and calls `RateLimiter.check(server_derived_athlete_id, class)` post-auth, or add a per-router dependency that debits READ/MUTATING buckets, wiring a single process RateLimiter on app state. | BYTES: async def list_available() -> ConnectionCatalog:
- [high] LIMIT-R2 src/wattwise_core/api/ratelimit.py:54 â The mandated read `120 req/min` and mutating `30 req/min` ceilings (LIMIT-R2) are defined but dead: `LimitClass.READ` and `LimitClass.MUTATING` are never enforced on any endpoint (no call site outside this module; agent_routes only uses `LimitClass.AGENT`). The OSS request-rate baseline LIMIT-R2 requires actually applies on general read and mutating endpoints. | FIX: Enforce READ on GET handlers and MUTATING on POST/PATCH/PUT/DELETE handlers across all feature routers (middleware or shared dependency), keyed on `principal.athlete_id` (AUTH-R3), so the LIMIT-R2 ceilings are exercised, not just declared. | BYTES: DEFAULT_LIMITS: Final[dict[LimitClass, int]] = {
    LimitClass.READ: 120,
    LimitClass.MUTATING: 30,
    LimitClass.AGENT: 20,
}
- [high] ERR-R9 src/wattwise_core/api/errors.py:255 â _on_http_exception maps a raised HTTPException to a problem-type SOLELY by HTTP status code and discards exc.detail entirely (detail=None, errors=()). performance.py's _precondition_unmet raises HTTPException(422, detail={"type":"analytics-precondition-unmet","errors":[{"code":"cp_insufficient_points",...}]}). Because 422 -> _STATUS_TO_SLUG = 'validation-error', the response is type=validation-error with NO machine errors[].code. ERR-R9/API-R30 require 422 analytics-precondition-unmet carrying a machine errors[].code (cp_insufficient_points / hrv_dsp_unavailable / pmc_seed_unavailable / trimp_*). The fail-closed analytics contract a client branches on is silently dropped. | FIX: Have these routers raise ProblemError('analytics-precondition-unmet', errors=[FieldError(code=...)]) (which the ProblemError handler renders intact) instead of HTTPException with a dict detail; or make _on_http_exception honor a structured detail carrying a catalog slug + errors[]. | BYTES: slug = _STATUS_TO_SLUG.get(exc.status_code, "internal-error")
    problem_type = CATALOG[slug]
    # Never echo a framework-supplied detail verbatim (ERR-R5): u
- [high] PAGE-R5 src/wattwise_core/api/routers/activities.py:232 â A tampered/expired cursor is raised as HTTPException(400, detail='invalid-cursor'), but _on_http_exception (errors.py:255) maps status 400 via _STATUS_TO_SLUG -> 'validation-error', so the emitted problem type is https://wattwise.app/problems/validation-error, NOT 'invalid-cursor'. PAGE-R5 / acceptance-check #3 require a tampered/expired cursor to yield 400 with problem type 'invalid-cursor'. A client branching on type (ERR-R3) can never detect cursor invalidity. | FIX: raise ProblemError('invalid-cursor') so the type URI is correct, instead of HTTPException with a string detail that the status-code remap overwrites. | BYTES: raise HTTPException(status_code=HTTPStatus.BAD_REQUEST, detail="invalid-cursor") from exc
- [high] API-R3 src/wattwise_core/api/app.py:85 â create_app mounts the performance, activities and agent routers (ROUTERS) but never wires their fail-closed override seams (require_read_scope/current_athlete_id/analytics_service/current_session in performance+activities; require_agent_scope/current_athlete_id/agent_engine/rate_limiter in agent_routes). Those default providers unconditionally raise 403/401/500 (performance.py:53-71, agent_routes.py:100-133) and are overridden ONLY in tests/integration. A production-booted app therefore returns 403/401/500 on EVERY /v1/performance/*, /v1/activities/* and /v1/agent/* request â the surfaces are non-functional as mounted. The repeated docstring claim 'the app factory wires (FastAPI dependency_overrides)' is not realized anywhere in src/. | FIX: In create_app, populate app.dependency_overrides for the performance/activities/agent seams (real read/agent scope gates from auth.py, server-derived current_athlete_id from the verified Principal, AnalyticsService/AgentEngine/RateLimiter/DB-session providers), or refactor these routers to use the shared deps.py gates (CurrentPrincipal/DbSession/require_scopes) the connections/imports/sync/onboarding routers already use. | BYTES: register_routers(app)
    return app
- [med] API-R48 src/wattwise_core/api/routers/activities.py:401 â decimation.algorithm is reported as 'minmax_lttb' but the actual decimation (_decimate_index, line 424-430) is a uniform fixed-stride sampler that does NOT preserve each channel's global min and max. API-R48/ANL-R8b and acceptance item 28 require that 'the global min and max sample of every channel survive' decimation (so the chart never contradicts max_power_w). The label and the implementation disagree; min/max can be dropped, contradicting the canonical max_power_w scalar. | FIX: Implement (or call) a real MinMaxLTTB that forces each channel's global min/max indices into the returned index set, or change the reported algorithm to reflect what is actually done. | BYTES: algorithm = "minmax_lttb" if len(idx) < length else "none"
- [med] API-R49 src/wattwise_core/api/routers/activities.py:476 â The map track reports decimation.algorithm='rdp' but uses the same uniform-stride _decimate_index sampler (line 473), not Ramer-Douglas-Peucker. API-R49 requires RDP, a shape-preserving simplifier that keeps turns/corners (eps derived from the max_points budget). Uniform stride drops corners and mislabels the algorithm. | FIX: Apply the RDP simplification (per ANL-R8b budget->epsilon mapping) for the map polyline, or correct the advertised algorithm. | BYTES: algo = "rdp" if len(points) < len(coords) else "none"
- [med] PAGE-R2 src/wattwise_core/api/routers/activities.py:238 â ActivityFilters declares no 'sort' or 'order' query parameters. PAGE-R2 mandates typed 'sort' (enum from a per-endpoint allow-list) and 'order' (asc|desc, default desc), and spec sec8.7 fixes the activities sort allow-list to start_time,duration,tss. _query_activities (line 291-294) hardcodes order_by start_time.desc()/activity_id.desc() with no way to request 'duration' or 'tss' sort or ascending order. | FIX: Add typed sort (enum start_time|duration|tss) and order (asc|desc, default desc) params, validate them, and thread them into the keyset query and the cursor binding. | BYTES: class ActivityFilters(BaseModel):

    frm: Annotated[_dt.date | None, Field(alias="from")] = None
    to: _dt.date | None = None
    sport: str | None = None
 
- [med] PAGE-R6 src/wattwise_core/api/routers/activities.py:288 â PAGE-R6 requires a cursor to be bound to the originating filter/sort params and a cursor supplied with conflicting sort/order/filter params to return 400 cursor-parameter-mismatch. The cursor encodes only {t,id} and is applied with no check that the accompanying filters match the cursor's originating request; the 'cursor-parameter-mismatch' type exists in the catalog (errors.py:92) but is never raised anywhere. | FIX: Encode the active filter/sort fingerprint into the (signed) cursor and, on decode, raise ProblemError('cursor-parameter-mismatch') when the request's filter/sort params differ. | BYTES: if f.cursor is not None:
        c_time, c_id = _decode_cursor(f.cursor)
        clauses.append(tuple_(Activity.start_time, Activity.activity_id) < (c_time, _ui
- [med] API-R11f src/wattwise_core/api/routers/agent_routes.py:162 â AgentAskRequest hardcodes response_length default 'standard' and _run_engine (line 343) forwards body.response_length verbatim. API-R11f requires the default to be 'the persisted setting, else standard' â when the client omits response_length the API must apply the athlete's persisted GET/PUT /v1/user-settings/response-length preference, not unconditionally 'standard'. The persisted setting is never consulted, so the per-athlete default is silently ignored. | FIX: Make response_length optional (None default); when absent, resolve the athlete's persisted response-length preference and only fall back to 'standard' if unset, then pass the resolved value to the engine. | BYTES: response_length: ResponseLength = "standard"
- [med] API-R22a src/wattwise_core/api/routers/agent_routes.py:378 â The SSE generator emits a 'status' frame then a single terminal done/error frame. API-R22a requires (a) a periodic ':'-comment heartbeat (~15s) to keep idle connections alive, and (b) deterministic resume on reconnect with Last-Event-ID â if the run finished return the terminal done immediately, signalling 'restart' vs 'done' via the first event type. Neither the heartbeat nor any Last-Event-ID/restart handling is implemented; the 'restart' member of the sse_event enum is never produced. | FIX: Emit periodic ':'-comment keepalive lines during the run and read Last-Event-ID to drive the resume/restart/done semantics required by API-R22a. | BYTES: async def _stream_answer(
    request: Request,
    engine: AgentEngine,
    athlete_id: str,
    body: AgentAskRequest,
    trace_id: str,
) -> AsyncIterator[s
- [med] API-R48 src/wattwise_core/api/routers/activities.py:398 â An explicitly-requested 'latlng' channel is silently dropped (the comprehension filters it out unconditionally). API-R48 keeps latlng in the streams allow-list ('a client wanting raw lat/lng samples index-aligned to a base requests it there'); an unknown/excluded token must be 422, but latlng is a VALID token here and must either be served or the schema must support it. Additionally StreamChannelOut.values is list[float|None] (line 135) which cannot represent [lat,lng] pairs, so latlng-in-streams is structurally unsupportable rather than served. | FIX: Either serve latlng index-aligned to base_values (with a value shape that can hold coordinate pairs) or reject 'latlng' on this endpoint as a 422 rather than silently omitting a requested, allow-listed channel. | BYTES: out_channels = {
        c.value: _channel_column(c, rows.get(c), idx) for c in selected
        if c is not StreamChannelName.LATLNG
    }
- [med] AUTH-R1 src/wattwise_core/api/app.py:141 â create_app mounts the performance/activities/agent routers but NEVER wires their fail-closed dependency seams (require_read_scope, current_athlete_id, analytics_service, require_agent_scope, agent_engine, current_session, rate_limiter): grep of app.py shows zero 'dependency_overrides' and zero references to those seam functions. The unwired defaults all raise HTTPException (403/401/500), and they are overridden only inside tests. As assembled by the production factory, every /v1/performance/*, /v1/activities/* and /v1/agent/ask request fails closed (403/401/500). The surface is non-functional in a real deployment (the routers depend on overrides the factory does not supply). Fail-closed avoids a security hole, but AUTH-R1/scope gating is realized via a seam the app never binds, so auth is effectively un-evaluable in production. | FIX: Bind the seam overrides in create_app (app.dependency_overrides for the scope/identity/service/session/limiter seams to the real auth gate, server-derived athlete id, AnalyticsService, DB session, and RateLimiter), or have the routers depend directly on the working deps.require_scope/authenticate seams as connections/sync/imports/onboarding already do. | BYTES: def register_routers(app: FastAPI) -> None:
    """Include every registered feature router, tolerating an empty registry.
- [med] DOC-R3 src/wattwise_core/api/routers/performance.py:74 â Performance, activities and agent ops gate via local stub dependencies (_Read=Depends(require_read_scope), _Agent) rather than a declared security scheme, so the emitted OpenAPI has NO per-operation `security` for any of these 16 ops (verified: only connections/imports/sync/onboarding ops carry security:[{bearer:[]}]). DOC-R3 requires every operation declare its required scopes via a security scheme + per-operation security; these ops expose neither bearer requirement nor required scopes in the contract. | FIX: Gate these routes with the real require_scopes(Scope.READ/AGENT) dependency (as connections does) so FastAPI emits per-op security, instead of the override-stub seams. | BYTES: _Read = Depends(require_read_scope)
- [med] PAGE-R5 src/wattwise_core/api/routers/activities.py:222 â The activities cursor is plain base64url-encoded JSON with NO signature and NO expiry. PAGE-R5 requires an opaque, SIGNED, server-issued cursor; a client can trivially construct/forge a cursor (decode, edit the keyset, re-encode) and it will be accepted, and _decode_cursor cannot detect tampering that still yields valid JSON. There is also no >=24h validity/expiry binding. | FIX: Sign the cursor payload (HMAC with the engine signing key) and verify the signature in _decode_cursor; reject a bad signature with ProblemError('invalid-cursor'). | BYTES: def _encode_cursor(start_time: _dt.datetime, activity_id: str) -> str:
    raw = json.dumps({"t": start_time.isoformat(), "id": activity_id}).encode()
    retur
- [med] ERR-R7 src/wattwise_core/api/errors.py:255 â activities._decode_cursor raises HTTPException(status_code=400, detail='invalid-cursor'). The handler maps 400 -> 'validation-error' whose catalog status is 422, and _render emits status_code=problem.problem_type.status (422). Verified: a 400 invalid-cursor request returns HTTP 422 with type 'validation-error', not 400 'invalid-cursor'. This violates ERR-R7 status-code usage (invalid-cursor MUST be 400) and PAGE-R5's 400 invalid-cursor contract; the type is also wrong. | FIX: Raise ProblemError('invalid-cursor') (and 'cursor-parameter-mismatch') directly instead of HTTPException; _STATUS_TO_SLUG[400] cannot disambiguate the three 400 problem types. | BYTES: slug = _STATUS_TO_SLUG.get(exc.status_code, "internal-error")
- [med] SCHEMA-R10 src/wattwise_core/api/routers/connections.py:276 â initiate has no response_model and returns a bare Python union; the emitted 200 schema is anyOf[ApiKeyNextStep,FileUploadNextStep] with NO OpenAPI `discriminator` (verified). SCHEMA-R10/DOC-R5 require the ConnectionNextStep union be discriminated on `kind`/`auth_archetype` so a typed client can branch without fixups; an undiscriminated anyOf forces the client to guess the member. | FIX: Declare a response_model with a Field(discriminator='kind') Union (Annotated[Union[...], Field(discriminator='kind')]) so OpenAPI emits a discriminator mapping. | BYTES: async def initiate(source: SourcePath) -> ApiKeyNextStep | FileUploadNextStep:
- [med] AUTH-R1 src/wattwise_core/api/app.py:151 â create_app never overrides the performance/activities/agent router seams (current_athlete_id, analytics_service, require_read_scope, current_session, agent_engine, rate_limiter) â only test fixtures do (rg shows dependency_overrides set solely under tests/). In the shipped app these defaults fail closed (raise 401/403/500), so the entire /v1/performance/*, /v1/activities/* and /v1/agent/ask surface is non-functional, and real bearer/scope auth (AUTH-R1/R3) is never enforced on them since they use stub gates rather than the authenticate dependency. | FIX: Wire app.dependency_overrides (or real Depends) in create_app/register_routers to bind authenticate->athlete id, the AnalyticsService, AgentEngine and RateLimiter for these routers, matching the connections/imports/sync wiring path. | BYTES: for router in _discover_routers():
        app.include_router(router)
- [med] API-R19 src/wattwise_core/api/errors.py:197 â The redaction module (redact_text/redact_payload, redaction.py) is defined to scrub PII/secrets from problem docs (API-R19) but is NEVER imported or applied anywhere in errors.py or the routers. Raise-site free text reaches detail/errors[].message unredacted: imports.py _rejected() passes adapter-supplied exc.reason into both detail and errors[].message, and agent_routes _problem_event passes exc.detail verbatim into the SSE error frame. Adapter/processor text can carry PII/secret substrings, so ERR-R5 'no leakage' and API-R19 'PII redaction in problem docs' are not enforced on the wire. | FIX: Apply redact_text() to detail and each errors[].message inside _Problem.to_body() (or _assemble/_render), and redact the SSE _problem_event body, so the existing redactor actually runs on every emitted problem document. | BYTES: def to_body(self) -> dict[str, Any]:
        """Render the RFC 9457 body (ERR-R2): the six required members + errors[]."""
        body: dict[str, Any] = {
    
- [med] SCHEMA-R4 src/wattwise_core/api/routers/agent_routes.py:152 â SCHEMA-R4 mandates request schemas reject unknown body properties (additionalProperties:false) to prevent silent acceptance of misnamed/forged fields (reinforcing AUTH-R3). AgentAskRequest (and its nested FollowUp at line 145) declare no model_config and default to pydantic extra='ignore', so unknown body props (e.g. a forged athlete_id or user_id) are silently accepted/dropped rather than rejected 422. The sibling routers (sync.SyncRunRequest, connections.ApiKeyCompleteRequest) correctly set ConfigDict(extra='forbid'); the agent body does not. | FIX: Add model_config = ConfigDict(extra='forbid') to AgentAskRequest and FollowUp so the OpenAPI schema is additionalProperties:false and forged/misnamed body props are rejected 422 (SCHEMA-R4). | BYTES: class AgentAskRequest(BaseModel):
- [med] PAGE-R5 src/wattwise_core/api/routers/activities.py:232 â A tampered/undecodable cursor is raised as HTTPException(400, detail='invalid-cursor'). The handler _on_http_exception maps by status only: 400 -> _STATUS_TO_SLUG = 'validation-error' (errors.py line 335). So the emitted problem type is 'validation-error', NOT the PAGE-R5/ERR-R8 required 'invalid-cursor'. Clients branching on type (ERR-R3) cannot detect an expired/tampered cursor. | FIX: Raise ProblemError('invalid-cursor') (400) instead of the bare HTTPException so the correct closed-catalog slug is emitted. | BYTES: raise HTTPException(status_code=HTTPStatus.BAD_REQUEST, detail="invalid-cursor") from exc
- [med] LIMIT-R5 src/wattwise_core/api/app.py:81 â LIMIT-R5 requires JSON request bodies be size-capped at a 256 KiB default with oversized bodies yielding 413 payload-too-large. No body-size middleware is installed by create_app and no router enforces a JSON cap; the only size enforcement that exists is the multipart upload cap inside imports.py (_read_capped). A large JSON body to e.g. POST /v1/agent/ask or POST /v1/connections/{source}/complete is not rejected with 413. (api__request_max_bytes defaults to ~32 MiB, the upload cap, not the 256 KiB JSON cap.) | FIX: Add an ASGI middleware (or per-route guard) that rejects JSON request bodies exceeding the 256 KiB default with ProblemError('payload-too-large') (413), enforced server-side from the streamed byte count, not a client-sent Content-Length (LIMIT-R6). | BYTES: install_error_handlers(app)
    _mount_liveness(app)
    app.include_router(_public_router())
    app.include_router(_auth_router())
    register_routers(app)
- [med] AUTH-R7 src/wattwise_core/api/app.py:85 â create_app never wires FastAPI dependency_overrides for the seam-based routers. agent_routes (require_agent_scope/current_athlete_id/agent_engine/rate_limiter), performance (require_read_scope/current_athlete_id/analytics_service), and activities (require_read_scope/current_athlete_id/current_session) ship with unbound default seams that fail closed (403/401/500). There is no dependency_overrides assignment anywhere in src/. As mounted by create_app, every /v1/agent/*, /v1/performance/*, and /v1/activities/* request returns 403/401/500 even with a valid full-scope token, so scope authorization (AUTH-R7/R11) is never actually evaluated against the principal for these surfaces. | FIX: In create_app/register_routers, set app.dependency_overrides for each router seam (require_read_scope->require_scopes(Scope.READ), require_agent_scope->require_scopes(Scope.AGENT), current_athlete_id->principal.athlete_id, current_session->get_db, services->real instances). Fail-closed is safe but the endpoints are otherwise non-functional and their scope gates untested at runtime. | BYTES: register_routers(app)
- [med] API-R19 src/wattwise_core/api/errors.py:240 â redaction.py provides redact_text/redact_payload for PII/secret masking in problem `detail`/`errors[].message` (its module docstring states it is 'the single, reusable redactor the API layer applies to the free-text it surfaces in a problem body'), but NO error handler or _Problem.to_body path calls it (grep shows redact* used only in observability/logging.py, never in api/errors.py). A caller-supplied `detail`/message that happens to echo an email, token, or key shape is emitted unredacted. API-R19 requires PII redaction in problem documents; ERR-R5 forbids leaking secrets/tokens in detail/errors[].message. The defense exists but is unwired on the problem-doc path. | FIX: Apply redact_text to problem.detail and each errors[].message inside _Problem.to_body() (or _assemble), mirroring the log-emit redactor so problem-doc and log redaction agree as the module intends. | BYTES: async def _on_problem_error(request: Request, exc: ProblemError) -> JSONResponse:
    """Render a raised :class:`ProblemError` (the primary error path)."""
    
- [med] API-R22a src/wattwise_core/api/routers/agent_routes.py:378 â API-R22a makes two MUSTs the SSE generator does not satisfy: (1) a periodic keepalive heartbeat (a `:`-comment line ~every 15s so idle connections are not dropped) â _stream_answer emits only a single status frame then awaits the whole run with no heartbeat, so a long agent run with no token events will be dropped by intermediaries; (2) Last-Event-ID resume semantics â the server MUST read a Last-Event-ID header on reconnect and resume / replay the terminal / signal restart|done; there is no Last-Event-ID handling anywhere (grep: none). Events do carry id: (0/error/done) per the resume requirement, but the resume read/replay path is absent. | FIX: Wrap the engine await in an async heartbeat (e.g. emit ':' comment frames on a ~15s interval while awaiting), and on a request carrying Last-Event-ID either replay from after that id, return the terminal done envelope if finished, or emit a `restart` first event per API-R22a(c). | BYTES: async def _stream_answer(
    request: Request,
    engine: AgentEngine,
    athlete_id: str,
    body: AgentAskRequest,
    trace_id: str,
) -> AsyncIterator[s
- [med] API-R11e src/wattwise_core/api/routers/agent_routes.py:266 â _followups_out hard-codes every suggested follow-up to kind='expand' with no target_ref (`SuggestedFollowupOut(kind='expand', label=label)`). API-R11e/Â§7 specify suggested_followups carry {kind, target_ref?, label} where kind can be reveal_numbers/drill bound to a specific observation_id/citation_id/callout_id target_ref so a thin client can render an addressable drill-down chip (the spec example is `{kind:reveal_numbers, target_ref:01J..., label:'See the numbers'}`). The projection drops the target_ref dimension and collapses all kinds to expand, so reveal_numbers/drill chips with a target_ref are never surfaced. The docstring concedes 'OSS does not bind a label to a specific target', which is the gap. | FIX: Carry the engine's typed follow-up kind + target_ref (observation_id/citation_id/callout_id) into SuggestedFollowupOut rather than forcing kind='expand' with target_ref=None. | BYTES: def _followups_out(answer: AgentAnswer) -> list[SuggestedFollowupOut]:
    """Project the engine's jargon-free follow-up prompts into reveal-numbers chips.
- [med] API-R11a src/wattwise_core/api/routers/agent_routes.py:289 â The degraded member's reason_text is a fixed constant `_DEGRADED_REASON_TEXT` ('I built this with what we have â a source is offline.') ignoring the engine's per-run reason and the requested `language`. API-R11a says reason_text is 'the warm human translation' of the typed caveat and API-R37 requires every athlete-facing string (explicitly including 'degraded/budget caveats') be returned in the selected language (en/de/ru). A `language:'de'` request still returns the English constant, and the caveat text never reflects which input was actually missing/substituted/stale. The structured coverage_caveat is passed through correctly, but the human reason_text is not localized or engine-derived. | FIX: Source reason_text from the engine's localized degraded reason (honoring body.language/Accept-Language per API-R37) instead of a single English module constant. | BYTES:     return DegradedOut(reason_text=_DEGRADED_REASON_TEXT, coverage_caveat=caveat)
- [med] ERR-R6 src/wattwise_core/api/routers/performance.py:176 â date_range from>to (PAGE-R8) and activities _validation() construct errors[] inside an HTTPException detail dict, but the production handler _on_http_exception forces errors=() (errors.py:258), so the 422 validation-error is emitted with an EMPTY errors[] â no parameter/pointer/code. ERR-R6 requires a 422 validation-error to carry a POPULATED errors[] with a parameter (query/path) or pointer (body). Additionally the member uses key 'detail' rather than the ERR-R6/ERR-R2 'message' (also moot since discarded). | FIX: Raise ProblemError('validation-error', errors=[FieldError(code='out_of_range', message='The start date needs to be on or before the end date.', parameter='from')]) so the populated errors[] reaches the client. | BYTES: raise HTTPException(
            status_code=HTTPStatus.UNPROCESSABLE_ENTITY,
            detail={"type": "validation-error", "errors": [{"parameter": "from"}]}
- [med] LIMIT-R2 src/wattwise_core/api/routers/agent_routes.py:455 â Rate limiting is enforced ONLY here, only for LimitClass.AGENT. No READ or MUTATING limiter is ever instantiated or .check()'d, and no global middleware applies one (grep of src/.../api shows the single .check call). The performance, activities, sync, imports, connections, and onboarding routers carry no rate-limit dependency, so the LIMIT-R2 read floor (120 req/min) and mutating floor (30 req/min) are never enforced and over-rate requests never receive 429 rate-limited + RateLimit-*/Retry-After (LIMIT-R3). LIMIT-R1 requires every endpoint rate-limited per athlete. | FIX: Add a rate-limit dependency (or ASGI middleware keyed on the server-derived athlete id) that calls RateLimiter.check with LimitClass.READ for GET routes and LimitClass.MUTATING for POST/PATCH/PUT/DELETE on every /v1 router, mirroring the agent path; emit the RateLimitHeaders on success and 429 on exhaustion. | BYTES: limiter.check(athlete_id, LimitClass.AGENT)
- [med] API-R16 src/wattwise_core/api/routers/agent_routes.py:416 â The streamed SSE error body omits the ProblemError.errors[] field. API-R16 requires the streamed error event to carry the SAME RFC 9457 problem body as the non-streamed contract. A streamed validation-error (_validate_request raises ProblemError('validation-error', errors=[FieldError(code='question_required', pointer='/question')])) therefore reaches the client without its errors[] pointer/code â a less-complete body than the synchronous path emits. | FIX: Include exc.errors (rendered via FieldError.to_dict()) as an 'errors' key when non-empty, so the streamed error body matches the synchronous problem document. | BYTES: return {
        "type": exc.problem_type.uri,
        "title": exc.problem_type.title,
        "status": exc.problem_type.status,
        "detail": exc.detail 
- [med] ERR-R7 src/wattwise_core/api/errors.py:335 â _on_http_exception maps any framework-raised 400 to slug 'validation-error' whose catalog status is 422, and _render uses problem_type.status for BOTH the HTTP status line and body status. A genuine framework 400 (ERR-R7: malformed request / invalid cursor / parameter conflict / bad OAuth state) is thus re-emitted as a 422 with type validation-error, collapsing the distinct 400 statuses ERR-R7 enumerates. (Body status==HTTP status, so ERR-R4 holds, but the 400â422 status rewrite changes the contractual meaning.) Likewise HTTPStatus.METHOD_NOT_ALLOWED maps to not-found (errors.py:339), turning a 405 into a 404. | FIX: Map framework 400 to a 400-status catalog member (e.g. a generic 'bad-request' slug or pass through the original status) rather than to the 422 validation-error type; preserve the originating HTTP status when normalizing framework HTTPExceptions. | BYTES:     HTTPStatus.BAD_REQUEST: "validation-error",
- [med] LIMIT-R5 src/wattwise_core/api/routers/imports.py:168 â The 413 cap is enforced AFTER FastAPI/Starlette has already parsed and buffered the multipart part into a SpooledTemporaryFile (spills to disk past the framework's small `max_part_size`/spool threshold). `_read_capped` re-reads the already-buffered spool, so a body far larger than 32 MiB is fully buffered before the handler's cap check runs â the docstring claim "enforced as the bytes stream in (never by trusting a client-sent length)... without buffering it whole" is false. A multipart part exceeding Starlette's `max_part_size` would also raise a framework error, not the catalog `413 payload-too-large`. | FIX: Enforce the cap before the body is buffered: reject on `Content-Length` > cap at request entry AND set Starlette `MultiPartParser` `max_part_size`/`max_file_size` (via a form-parsing middleware or stream the raw `request.stream()` yourself) so an oversize upload is rejected mid-stream with the catalog `413`, not after a full spool. | BYTES: async def _read_capped(file: UploadFile, max_bytes: int) -> _Upload:
    """Stream the upload into memory, failing closed at the cap â ``413`` (LIMIT-R5).

    
- [med] AUTH-R11 src/wattwise_core/api/routers/connections.py:274 â `POST /v1/connections/{source}/initiate` (and `complete`, line 296) require BOTH `read` AND `write`. Spec API-R43/API-R44 specify these mutating endpoints as `(write)` only; AUTH-R11 maps mutating endpoints to `write` plus endpoint-specific scope (none here). Requiring an extra `read` scope is over-restrictive: a spec-valid `write`-only token is wrongly rejected `403 insufficient-scope`. | FIX: Drop `Scope.READ` from both initiate and complete: `require_scopes(Scope.WRITE)` per API-R43/R44/AUTH-R11. | BYTES: dependencies=[Depends(require_scopes(Scope.READ, Scope.WRITE))],
- [med] API-R45 src/wattwise_core/api/routers/connections.py:56 â The Connections router omits `POST /v1/connections/{connection_id}/reconnect` (API-R45), a non-commercial MUST in Â§7C for `reauth_required`/`error` connections returning the same `ConnectionNextStep` union with atomic credential replace + history preserved. It is not OAuth-gated (unlike the commercial `POST /v1/connections` + callback), so its absence leaves a stuck/errored api_key connection with no in-contract recovery path. | FIX: Add the `reconnect` route reusing the api_key initiate/complete flow against an existing connection, atomically replacing `credential_ref` and flipping status to `connected` only after a successful probe (AUTH-R17), preserving canonical history. | BYTES: router = APIRouter(prefix="/v1/connections", tags=["connections"])
- [med] ERR-R6 src/wattwise_core/api/routers/performance.py:176 â from>to and bad base/max_points/channel violations raise HTTPException(422, detail={...errors:[{parameter:...}]}), but _on_http_exception drops the dict and emits validation-error with NO errors[] array. ERR-R6 requires a 422 validation-error with a POPULATED errors[] using parameter for query/path violations. Same pattern in activities.py _validation (line 443) and date_range. The offending parameter name a client needs is lost. | FIX: Raise ProblemError('validation-error', errors=[FieldError(code='out_of_range', message=..., parameter='from')]) so the errors[] survive into the rendered problem. | BYTES: raise HTTPException(
            status_code=HTTPStatus.UNPROCESSABLE_ENTITY,
            detail={"type": "validation-error", "errors": [{"parameter": "from"}]}
- [med] API-R22a src/wattwise_core/api/routers/agent_routes.py:394 â The SSE generator _stream_answer emits status then a single terminal frame; it never emits the periodic ':'-comment heartbeat API-R22a says the server MUST emit (~every 15s) to keep idle connections alive, and never reads Last-Event-ID nor implements the restart/done resume semantics API-R22a mandates (no rg hit for last-event-id/resume/restart). A long agent run with no intermediate tokens can be dropped by an intermediary, and a reconnect cannot resume. | FIX: Interleave heartbeat ':'-comment frames while awaiting the engine, and handle the inbound Last-Event-ID header to either resume, replay the terminal done envelope, or signal restart via the first event's event: type per API-R22a(a)-(c). | BYTES: yield _sse_event("status", {"status": "working"}, event_id="0")
    try:
        if await request.is_disconnected():
            return
        answer = await _
- [med] PAGE-R2 src/wattwise_core/api/routers/activities.py:238 â GET /v1/activities exposes no typed sort/order query params; _query_activities hardcodes order_by(start_time desc, activity_id desc). PAGE-R2 mandates a typed sort enum (per-endpoint allow-list, here start_time|duration|tss per digest Â§4 and spec 8.7) and order (asc|desc, default desc). The documented sort allow-list is unimplemented, so clients cannot sort by duration or tss. | FIX: Add sort: Literal['start_time','duration','tss'] = 'start_time' and order: Literal['asc','desc'] = 'desc' to ActivityFilters and apply them (with the matching tie-broken keyset) in _query_activities; encode the chosen sort/order into the cursor. | BYTES: class ActivityFilters(BaseModel):

    frm: Annotated[_dt.date | None, Field(alias="from")] = None
    to: _dt.date | None = None
    sport: str | None = None
 
- [med] PAGE-R6 src/wattwise_core/api/routers/activities.py:288 â A cursor is decoded and applied without checking it was issued for the same filter/sort parameters. PAGE-R6 requires that a cursor supplied together with conflicting sort/order/filter params yields 400 cursor-parameter-mismatch. The 'cursor-parameter-mismatch' catalog entry exists (errors.py:92) but is never raised anywhere in src/. A client mixing a cursor with changed filters silently gets wrong/unstable pages. | FIX: Bind the originating filter+sort hash into the cursor payload and, on decode, reject with ProblemError('cursor-parameter-mismatch') when the current request's filter/sort differs. | BYTES: if f.cursor is not None:
        c_time, c_id = _decode_cursor(f.cursor)
        clauses.append(tuple_(Activity.start_time, Activity.activity_id) < (c_time, _ui
- [med] PAGE-R5 src/wattwise_core/api/routers/activities.py:222 â The cursor is plain base64-encoded JSON with NO signature/HMAC (no hmac/sign/secret in the module). _decode_cursor only catches malformed base64/JSON; a structurally valid but TAMPERED cursor (e.g. an altered 't' date the client fabricates) decodes successfully and is applied to the query. PAGE-R5 requires the cursor be opaque, SIGNED, server-issued, with a tampered cursor rejected (400 invalid-cursor) and the client unable to construct cursors. | FIX: Sign the cursor payload (HMAC with the engine signing key, e.g. itsdangerous or hmac) and verify the signature on decode, rejecting an unsigned/forged cursor as invalid-cursor. | BYTES: def _encode_cursor(start_time: _dt.datetime, activity_id: str) -> str:
    raw = json.dumps({"t": start_time.isoformat(), "id": activity_id}).encode()
    retur
- [med] API-R37 src/wattwise_core/api/routers/agent_routes.py:344 â The agent only honors the body language field (defaulting to 'en'); it never reads the Accept-Language request header (no accept-language hit anywhere in api/). API-R37 specifies the per-request override MAY come via Accept-Language and that the body language field SHALL take precedence over Accept-Language when both are present, and that a missing per-request value falls back to the persisted setting (then default_language). Here Accept-Language is ignored and there is no persisted-setting fallback at the API layer. | FIX: Resolve locale as body.language -> Accept-Language header -> persisted user-settings language -> default_language, instead of body.language or 'en'. | BYTES: locale=body.language or "en","
}