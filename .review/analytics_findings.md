## Analytics: 4 findings (2 high, 2 med, 0 low)
- [high] PMC-R5 src/wattwise_core/analytics/service.py:301 â AnalyticsService.pmc() builds the daily-load series only over [from_date, to_date] and forwards it to the pure pmc() with window=None and seed defaulting to None. With window=None, the pure function treats index 0 (= from_date) as the athlete's first-ever day and zero-seeds (_resolve_seed returns (0,0) when d0==0). Every real caller passes no seed (api/performance.py:213, agent/tools.py:117, agent/capabilities.py:152, and capabilities.py:305 which calls pmc(athlete_id, day, day)). So any mid-history PMC window is SILENTLY ZERO-SEEDED, producing CTL/ATL near zero regardless of true training history -- exactly the wrong-but-plausible fitness number PMC-R5 forbids ('It MUST NOT silently zero-seed a mid-history window'). The pure function's NOT_SEEDED guard is defeated because the service never passes a window or derives the carried-forward seed (CTL(d0-1), ATL(d0-1)) from history strictly before from_date (PMC-R3). | FIX: In the service, either (a) extend daily_load_series back to the athlete's first day and compute from origin (then slice to [from_date,to_date]), or (b) load/derive the persisted carried-forward seed (CTL(from_date-1), ATL(from_date-1)) and pass it with window=(d0,d1) so the pure pmc() returns Unavailable(NOT_SEEDED) when no seed exists instead of zero-seeding. | BYTES: return _pmc.pmc(series, seed=seed)
- [high] MMP-R1 src/wattwise_core/analytics/mmp_cp.py:273 â MMP-R1 defines MMP(d) literally as the max over start offsets of mean(P(t0 .. t0+d-1)) over EXACTLY-d-second windows (matching the field-standard 'best average power over any d-second window'). _at_least_envelope instead maximizes over all window lengths L >= d ('at-least-d power-duration curve', confirmed by the brute-force oracle in tests/property/test_mmp_cp_property.py:69 and MMPWindow.window_len_s >= duration_s). For non-monotone data a window of length L>d can have a higher mean than every exactly-d window, so MMP(d)/best_effort(d) can report a value HIGHER than the true best d-second power -- a wrong-but-plausible number diverging from MMP-R1's formula and the standard MMP definition. (The implementation adopted at-least-d to make MMP-R3 monotonicity hold by construction, which the spec's literal exactly-d formula does NOT guarantee -- a genuine spec/code tension, but the code departs from the stated MMP-R1 formula.) | FIX: Either compute exactly-d peaks per MMP-R1 (max over exactly-length-d windows) and accept that monotonicity is then an empirical invariant to test rather than 'by construction', or get MMP-R1's formula amended to explicitly define the at-least-d envelope. Do not silently report a >d-second window's mean as MMP(d). | BYTES: for length in range(d, n + 1):
- [med] LOAD-R3 src/wattwise_core/analytics/service.py:276 â daily_load_series (the PMC input, LOAD-R1) computes per-activity load ONLY via coggan (the power-TSS path). LOAD-R3 mandates the deterministic priority power_tss -> hr_load -> Unavailable, and LOAD-R1 requires per-activity load = power TSS when available, ELSE HRLoad. An HR-only activity (no power channel) makes coggan return Unavailable(MISSING_REQUIRED_INPUT,'no power channel'), so the day contributes None (surfaced unknown-load) instead of the Banister hr_load that step 2 of LOAD-R3 requires. The service never invokes trimp.banister_hr_load as the fallback, so an athlete riding without a power meter (or after a sport change -- ANL-R13 sport-agnostic continuity) accrues no load and PMC decays to ~0, breaking the continuous-load-series guarantee. | FIX: In daily_load_series, when coggan/power-TSS is Unavailable for an activity that has an HR channel + effective HR_max/HR_rest, fall back to banister_hr_load per LOAD-R3 step 2 and add its value to the day, labeling load_model='hr_load'; only contribute None when neither power_tss nor hr_load is computable. | BYTES: bundle = await self.coggan(str(act.activity_id))
- [med] ANL-R13 src/wattwise_core/analytics/service.py:315 â power_curve() aggregates the mean-maximal-power curve over ALL activities in the date range that carry a POWER channel, with no filter on act.sport, and calls _mmp.mmp(...) with the default sport='cycling' (act.sport is not passed). ANL-R13 / MMP-R4 require sport-PARTITIONED aggregates for sport-specific metrics: a non-cycling activity that happens to carry a mechanical-power channel would be pooled into the same single power curve and mislabeled sport='cycling' in lineage, pooling incommensurable sports into one scalar. The CP fit then consumes this cross-sport-pooled curve (critical_power, line 325). | FIX: Partition the aggregate by act.sport (only pool activities whose sport shares the cycling-power model) and pass act.sport into _mmp.mmp so lineage carries the true resolved sport; return per-sport-partitioned curves rather than one pooled curve. | BYTES: for d, res in _mmp.mmp(resample_to_1hz(power)).items():
